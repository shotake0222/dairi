{
  "name": "sodatsukake",
  "version": "0.1.0",
  "private": true,
  "description": "わけたま（Waketama） - NFCタグ起点・会話育成型キャラクターサービス（Cloudflare Workers / Durable Objects / D1 / Workers AI）",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env=\"\" --var APP_VERSION:$(git rev-parse --short HEAD)$(git diff --quiet || echo -dirty) --var BUILT_AT:$(date -u +%Y-%m-%dT%H:%M:%SZ)",
    "db:migrate:local": "wrangler d1 migrations apply nfc-companion-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply nfc-companion-db --remote",
    "admin:passcode": "wrangler secret put ADMIN_PASSCODE --env=\"\"",
    "secrets:list": "wrangler secret list --env=\"\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "node tools/e2e.mjs",
    "persona:fixtures": "node tools/make-persona-fixtures.mjs",
    "persona:check": "node tools/make-persona-fixtures.mjs && node tools/persona-runtime-check.mjs",
    "brand:assets": "python3 tools/make_brand_assets.py",
    "deploy:staging": "wrangler deploy --env staging --var APP_VERSION:$(git rev-parse --short HEAD)$(git diff --quiet || echo -dirty) --var BUILT_AT:$(date -u +%Y-%m-%dT%H:%M:%SZ)",
    "setup:staging": "node tools/setup-staging.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.4",
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.129.0"
  }
}
