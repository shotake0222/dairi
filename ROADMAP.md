# わけたま 開発ロードマップ

最終更新: 2026-09-23（複数人のメタバース＝フェーズ25、キャラクター10種族追加と声の作り分け＝フェーズ24、
法人へ渡す写しを会話と切り離し、個別提供の同意を追加＝フェーズ23。
2026-09-22 に法人向けページへ料金を掲載＝フェーズ22。
その前日に、実機デバッグ手順書、読み上げの画面統合、CSPのnonce化、
エコー対策、年齢への配慮、不適切な入力への対応方針を実装）

本番ドメイン（waketama.com）が動き出すと、これまでのように手元だけで完結しなくなります。
実機で起きた不具合を追える状態を先に作ってから機能を足す、という順序でまとめました。

---

## 現在地

**動いているもの**: NFCタップ→召喚→会話、性格の育成と成長グラフ、長期記憶（Vectorize）、
分身同士のお散歩（オプトイン制・自動実行）、出会いの図鑑、分身のホーム画面、人格のエクスポート/インポート、
持ち主トークンによる保護、分身の削除、PWA、OGP。テストは84件（vitest）＋実機E2E。

**2026-09-12に実装したもの**: 構造化ログと `/api/health`、版数の刻印、保存に依存しないレート制限、
その場限りの通話モード（SSEストリーミング・通話UI）、音声入力（Web Speech API + Whisper）、
読み上げAPI、引き継ぎコード、ステージング環境。テストは114件（vitest）＋実機E2E。

**まだ無いもの**（2026-09-21 時点）: 利用規約・プライバシーポリシーの**弁護士等による確認**
（事業者名・連絡先・管轄裁判所の空欄は埋め終わった。`npm run legal:check` は空欄0件）、
実機での動作確認全般（音声・視線入力・キーホルダー・CSP）、読み上げの既定選択（標準/高音質のどちらを
既定にするか）、キーホルダーの試作・実写真への差し替え、apexのDNS整理、決済の導入、
法人向け統計提供の運用整備。いずれも「実機」「専門家」「Cloudflareダッシュボード」「事業判断」の
どれかが要り、コードだけでは完結しない。

> 以前ここに並べていた「iOS向けのホーム画面追加の案内」「性格更新ロジックのチューニング」
> 「Service Workerの緊急停止手段」は、いずれも実装済み（順に 16-2 / 16-3 / 10-4）。
> 「実機デバッグ手順書」「読み上げの画面統合」「CSPのnonce化」「エコー対策」「年齢への配慮」
> 「不適切な入力への対応方針」も2026-09-21に実装済み（順に 0-5 / 1-2 / 10-8 / 6-3 / フェーズ5×2）。
> **この段落は実装より先に古くなる。** 直したときに一緒に直すこと。

---

## フェーズ0: 本番を「デバッグできる」状態にする（最優先）

機能追加より先にここを固めないと、実機で起きた不具合が「再現できません」で終わります。
ドメイン反映後、最初のデプロイと同時に着手する想定です。

### 0-1. Workers Logs を有効化 ✅ 設定済み（要デプロイ）

`wrangler.toml` に `[observability]` を追加済みです。次回のデプロイで有効になり、
Cloudflareダッシュボードから過去のリクエストとログを検索できるようになります。

リアルタイムで追う場合は以下。実機を触りながら別画面で流しておくのが基本形です。

```bash
npx wrangler tail --format pretty
npx wrangler tail --status error          # エラーだけ
npx wrangler tail --search "characterId"  # 文字列で絞る
```

### 0-2. リクエストIDと構造化ログ ✅ 実装済み

現状のコードは失敗を握りつぶす設計（AIやVectorizeが落ちても会話は続く）にしてあります。
これは体験としては正しいのですが、**失敗しても何も残らない**ため、
「なんとなく記憶が弱い気がする」といった不具合の原因追跡ができません。

- 握りつぶしている `catch` に `console.warn` を入れ、どこで何が落ちたかを残す
- 1リクエストにIDを振り、DO・AI・Vectorizeのログを串刺しで追えるようにする
- 個人情報（会話本文）はログに出さない。出すのは種別・所要時間・成否のみ

### 0-3. `/api/health` と バージョン表示 ✅ 実装済み

D1・Vectorize・AIそれぞれに疎通確認をかけ、結果をJSONで返すエンドポイント。
「Vectorizeのインデックスを作り忘れていて記憶が保存されていなかった」類の事故は、
これがあると1秒で分かります。

あわせて、デプロイのバージョン（コミットハッシュ等）をレスポンスヘッダと `/api/health` に載せます。
**PWA化した以上これは必須です**。端末に古いService Workerやキャッシュが残っている状態で
「直したはずのバグが直っていない」と悩むのを防げます。

### 0-4. ステージング環境 ✅ 実装済み（初回のD1作成のみ手作業）

本番のD1に混ざると、テストで作った分身が「お散歩」の相手候補として実ユーザーに出てしまいます。
`wrangler.toml` に `[env.staging]` を追加し、別Worker・別D1・別Vectorizeインデックスに分けます。

```bash
npx wrangler deploy --env staging   # staging.waketama.com などに出す
```

### 0-5. 実機デバッグの手順書 ✅ 実装済み（2026-09-21）

NFC・カメラ・マイク・PWAインストールは実機でしか確認できません。手順を
[docs/DEVICE_DEBUG.md](docs/DEVICE_DEBUG.md) にまとめました。

- iPhone: Mac の Safari →「開発」メニューから実機のSafariに接続してコンソールを見る
- Android: Chromeで `chrome://inspect` から実機を開く
- NFCタグの書き込み: iPhoneは「NFC TagWriter」、Androidは「NFC Tools」
- あわせて、カメラ・マイク・通知の権限がiOS/Androidで挙動が違う点、PWAのキャッシュが
  更新を邪魔しがちな点、検証用NFCタグを作るときの注意（フィラー品UIDの見分け方）も記載。
  実機を持っていない私自身は動作確認できていないので、実際に実機で試して手順の過不足が
  あれば直してください。

運用担当者向けの障害切り分け（[OPERATIONS.md](docs/OPERATIONS.md) §6）とは相互リンク済み。

### 0-6. Service Worker の緊急停止手段 ✅ 実装済み（10-4で解消）

PWAで最も怖い事故は、壊れたService Workerが端末に居座って更新を配信できなくなることです。
現状のSWはHTMLとAPIをキャッシュしない設計なので危険度は低いのですが、保険として
「登録解除だけを行うSW」を配信できる状態（kill switch）を用意しました。詳しくは10-4を参照。

### 0-7. コスト上限とレート制限 ✅ 実装済み

Workers AIは呼ぶたびに課金されます。現状の防御は「連投ガード（1.2秒）」と
「お散歩のクールダウン（20時間）」だけで、**1人が延々と話し続けるコストは止められません**。

- 1分/1日あたりの上限を設ける（Durable Object内でカウント）
- Cloudflare側の課金アラートを設定する
- 上限に当たったときの文言を、キャラクターの世界観を壊さない形で用意する（「少し眠くなってきた…」等）

---

## フェーズ1: 音声でのやり取り

現状は**読み上げ（出力）だけ**が実装済み（Web Speech APIの音声合成、性格で速さと高さが変わる）。
足りないのは音声入力と、会話として成立させるつなぎ込みです。

### 1-1. 音声入力 ✅ 実装済み（実機での確認は要）

二段構えを推奨します。

1. **Web Speech API（`SpeechRecognition`）**: 無料・低遅延。Chrome系で良好。まずこれを使う
2. **Workers AI Whisper へのフォールバック**: `MediaRecorder` で録音してWorkerへPOSTし、
   サーバー側で文字起こし。iOS Safariなど1が不安定な環境向け

**デバッグ観点**（ここは実機でしか分かりません）:
- マイク権限の拒否・取り消し時に、無言で壊れず案内が出るか
- iOS Safariは仕様変更が多い。実機での確認を前提に、失敗したらテキスト入力へ静かに戻す
- 無音・雑音のみのときにAIを呼ばない（コストと誤爆の防止）
- 方言や固有名詞の誤認識をどう扱うか（確定前に編集できるUIにするか）

### 1-2. 読み上げ品質の向上 ✅ 画面からの選択制として実装済み（2026-09-21）

ブラウザ標準の音声合成は端末によって声が大きく変わり、機械的です。
Workers AI の音声合成モデル（MeloTTS）を、選べる選択肢として`/chat`・`/talk`・`/call`・`/eyes`の
4画面すべてに追加しました（共通部品: `public/voiceSpeak.js`）。

- **生成した音声のキャッシュ**: ブラウザのメモリ内キャッシュ（同じ台詞なら再生成しない）。
  サーバー側には音声を保存しない方針は変えていない（`src/voice.ts` の既存方針どおり）
- **標準／高音質の切り替え設定**: 各画面に「声質：標準／高音質」の切り替えを追加。
  `localStorage`（`sodatsukake_voiceQuality`）で端末ごとに覚える。**既定は標準のまま**
- **デバッグ観点への対応**: iOSの「ユーザー操作なしの音声再生禁止」対策として、
  各画面の最初のタップ（通話開始・送信・かざして話すの開始など）の中で
  無音の`<audio>`再生を1回はさむ「解錠」処理を入れた（標準音声側で既にあった
  `SpeechSynthesisUtterance`の無音発話と同じ狙い）
- 生成・再生のどちらかに失敗したときは、黙って標準音声にフォールバックする
  （無音のまま会話が止まるのを避けるため）

**まだ残っている判断**: どちらを既定にするかは、実際に日本語で聞き比べてから決めるべき事項として
残っている。私はこの環境で音声を実際に聞いて品質を判断できないため、既定は変えていない。
また、現状のMeloTTSは声質が1種類しかなく、ブラウザ標準側にある「種族ごとに違う声を当てる」演出は
高音質モードでは効かない（`voiceSpeak.js` にコメントで明記）。実際に聞いてみて、
品質が良ければ既定を高音質に倒す・声のバリエーションを増やす、といった判断をお願いします。
なお `/eyes`（視線で話しかける）は、視線・スイッチ操作という特性上、既存の「設定」オーバーレイに
チェックボックスとして追加するにとどめ、視線スキャンの選択肢自体は増やしていない。

### 1-3. ハンズフリー会話ループ ✅ 実装済み

「話す→文字起こし→返答→読み上げ→また話す」を自動で回すモード。
フェーズ2のストリーミングと組み合わせると、後述の「電話」体験になります。

---

## フェーズ2: その場限りのチャットモード（Jiffcy的な「テキスト通話」）

参考にされた **Jiffcy（ジフシー）** の要点は、①電話のように“繋いで”話す、
②入力中の文字が1文字ずつ相手に見える、③既読が無い、④履歴が残らない、の4点です。
これを「相手＝自分の分身」に置き換えると、**「分身と電話する」体験**になります。

### 2-1. サーバー側: 何も残さない会話 ✅ 実装済み

`POST /api/chat` に `ephemeral: true` を足すのではなく、
**専用の経路を用意するほうが安全**です。理由は、既存のchat()は性格更新・履歴追加・
記憶保存・ディレクトリ同期まで一気に行うため、条件分岐で「書かない」を足すと
将来の変更で簡単に漏れるからです。

設計方針:

- Durable Object は **読み取り専用**（`getState()` のみ）で使う
- AI呼び出しはWorker側で行い、`storage.put` も `storeMemory` も一切通らない経路にする
- 記録しないもの: 会話本文、記憶（Vectorize）、性格の変化、やり取り回数、成長段階、最終訪問日時
- 参照はする: 現在の性格と口調（そうしないと「自分の分身」ではなくなるため）。
  「何も思い出さないモード」は後から選択制で足せる
- **レート制限の作り直しが必要**: 現在の連投ガードは `lastMessageAt` の書き込みに依存しています。
  何も書かないモードでは効かないので、DOインスタンスのメモリ上でカウントする方式に変えます

### 2-2. 「本当に残っていない」ことをテストで担保する ✅ 実装済み

この機能は「残らないと言ったのに残っていた」が最悪の事故です。信用を直接壊します。
実装より先に、この検証を書く価値があります。

- その場限りモードで会話 → DOのストレージが**1バイトも変化していない**ことを確認
- `env.AI.run` / `MEMORY_INDEX.upsert` のスパイで、記憶保存が**呼ばれていない**ことを確認
- 会話後にエクスポートしたパッケージに、その会話が含まれていないことを確認
- 会話後の「今日の分身レポート」「出会いの図鑑」に痕跡が出ないことを確認

### 2-3. 応答のストリーミング ✅ 実装済み（その場限りモードのみ。通常チャットは未対応）

Jiffcyの「1文字ずつ」に相当する部分。今は返答が出来上がってから一括で表示しているので、
待ち時間が沈黙になります。Workers AI のストリーミング応答をSSEで流し、
分身の言葉が少しずつ現れるようにします。

- その場限りモードはDOに書き込まないため、Worker側で完結でき、実装が素直です
  （通常モードは保存処理があるため、後追いで対応）
- Service Workerは `/api/` を素通しする設計になっているので、SSEを壊しません（対応済み）
- **デバッグ観点**: 途中で回線が切れたとき、書きかけの吹き出しが残らないようにする

### 2-4. 「通話」UI ✅ 実装済み

- 通話画面らしい見た目（背景を落とし、分身を中央に、経過時間を表示）
- 入力中の文字が分身側にも見えている演出（分身が「聞いている」感じを出す）
- 退出時に画面上のログを完全に消去し、戻るボタンでも復元されないようにする
- **「この会話は保存されません」を、開始時と終了時にはっきり伝える**（信頼の要）
- 音声（フェーズ1）と組み合わせると、そのまま音声通話になります

---

## フェーズ3: 所有権の引き継ぎ（現状の弱点）

いまの持ち主トークンはlocalStorageにしか無いため、**機種変更やブラウザのデータ削除で
自分の分身の所有権を永久に失います**。育てるサービスとしてこれは痛い。

### 3-1. 引き継ぎコード ✅ 実装済み

アカウント登録を入れずに解決する現実的な案です。

- 持ち主の端末で「引き継ぎコード」を発行（有効期限つきの短い文字列）
- 新しい端末でそのコードを入力すると、持ち主トークンが移る
- 発行時に「このコードを他人に渡すと分身を乗っ取られます」と明示する

将来的な本格的アカウント（Cloudflare Access等）は、収益化やBtoB提供の段階で検討。

---

## フェーズ4: 体験の質を上げる

- **性格更新ロジックのチューニング**: 係数はまだ仮の値。実ログを見ながら調整（要: フェーズ0の観測性）
- ~~**signalExtractor の高度化**~~: 実施済み。話題の新規性を「疑問符が付いているか」ではなく
  「これまで出てこなかった語か」で測るようにした（分身が既に持っている語彙と突き合わせる）。
  絵文字の感情、強調の段階、笑い表現の判定も見直した。AI呼び出しは増やしていない
- ~~**会話モデルの見直し**~~: 実施済み。ただし「モデルが小さいこと」は主因ではなく、
  **直近の会話をAIに渡していなかったこと**が「会話が頭悪い」の正体でした（詳細は下のフェーズ6）。
  モデル選定は `src/ai/modelPolicy.ts` に集約し、`--var CHAT_MODEL:<ID>` で差し替えられるようにしてあります。
  残っているのは**実会話を見ながらの評価**です（どのモデルが日本語のキャラクター演技に向くかは、
  ベンチマークより実際の会話ログで判断すべきなので、しばらく使ってから決めるのが妥当です）
- **iOS向けの「ホーム画面に追加」案内**: iOS Safariは `beforeinstallprompt` 非対応のため、
  現在Androidにしか案内が出ていない。共有メニューからの手順を出す
- ~~**出会いの図鑑のページング**~~: 実施済み。12件ずつ読み込む
- ~~**記憶削除の取りこぼし**~~: 実施済み。1回のクエリで済ませていたため200件を超える分が残っていた。
  無くなるまで繰り返すようにした（上限5000件。超えたらログに残る）

---

## フェーズ6: かざして話す／会話品質（2026-09-13 実装）

### 6-1. 会話が噛み合わない原因の特定と修正（済）

「会話が頭悪い」の原因はモデルの大きさではありませんでした。`chat()` がAIに渡していたのは
**システムプロンプトと、今回の1発言だけ**で、会話履歴を一切渡していませんでした。
文脈の代わりに使っていた `memorySummary` は、各発言を40文字に切り詰めた1行要約の羅列で、
モデルから見ると毎ターンが初対面でした。直したのは次の3点です。

- **直近のやり取りを原文で保持し、`messages` に user / assistant として積む**（`recentTurns`）
- **「覚え書き」の蓄積**（`src/ai/reflection.ts`）: 数ターンごとに、相手について長く覚えておくべき事実だけを
  AIに蒸留させて溜め、常にプロンプトに載せる。会話を重ねるほど厚くなるので、**育てるほど話が通じるようになる**
- **成長段階で文脈量を変える**（`contextBudgetFor()`）: 生まれたては短く素朴に、育つほど思い出す量と
  話せる長さが増える。モデルを弱くして「バカな時期」を作るのではなく、渡す記憶の量で差をつけている

あわせて、モデルの選定・フォールバック連鎖・環境変数での差し替えを `src/ai/modelPolicy.ts` に集約しました。

### 6-2. かざして話す（済）

カメラのライブビューを表示したまま、同じ画面でマイクを開き、撮像画像を表示したまま応答を
重畳表示＋読み上げする多ターンのモード（`/talk`）。サーバーは文章だけでなく**演出スクリプト**を返し、
ブラウザがそれを実行します。分身ごとの固有の声（characterIdから一意に決まる）も実装しました。
「👁 これ見て」を押したときだけ、その瞬間の1枚を視覚モデルに渡して会話の文脈に混ぜます（画像は保存しない）。

特許ファミリーの構成要件との関係は `特開2024-129022_クレーム対応メモ.md` の6節を参照してください。

### 6-3. 残っていること

- ~~**エコー対策（コード側）**~~ 実施済み（2026-09-21）: `/talk`・`/call`双方の`speak()`に
  `ttsPlaying`という旗を持たせ、①読み上げを始める瞬間にマイクが開いていたら問答無用で閉じる、
  ②読み上げ中は`startListening()`自体が何もせず即座に戻る、の二重の締め方にした。
  従来も「`await speak()`してから聞き直す」という順序で大枠は守られていたが、
  `onend`が発火しないブラウザ向けの時間切れフォールバック（実際の再生が終わる前に
  次へ進んでしまいうる）があるため、`speak()`自身の責務として明示的に締めるようにした。
  **実機確認は依然として必要**（この修正が実際にエコーを止めているかは、コードレビューでしか
  確認できていない。iOS Safari / Android Chromeでの実地確認は`docs/DEVICE_DEBUG.md`の手順で）
- **会話品質の再評価**: 6-1の修正後に、実際の会話でどれだけ良くなったかを見てからモデルを決める
- ~~**覚え書きの見え方**~~ 実は既に対応済みだった（フェーズ8「覚え書きの書き直し」、2026-09-13）。
  `/profile`に「1. あなたが書く土台」「2. 会話から覚えたこと」の2段で表示・編集できるようになっている。
  この6-3の項目自体が、それより前の版のROADMAPを引きずっていた重複記載だったので、ここで消し込む

---

## フェーズ7: 人格データ基盤・アクセシビリティ・事業面（2026-09-13 実装）

### 7-1. 同意を土台に置いた（済）

属性の取得・統計利用・出品を、**用途ごとに分けた同意**の下に置いた（`src/persona/consent.ts`）。
既定はすべてオフ。文面を変えたら版を上げて取り直す。集計用レジストリ（D1）には
**同意した分身しか行が存在せず**、同意を外すと行ごと消える。

### 7-2. サイコグラフィックとセグメンテーション（済）

- 語の出現から関心を更新する無料の層（`analysis/textMining.ts`）と、
  数ターンごとにAIで価値観を推定する層（`analysis/psychographics.ts`）の二段構え
- どちらも指数移動平均で少しずつ動かす（1回の会話で人物像が入れ替わらないように）
- 性格＋価値観＋関心から7つのセグメントへ分類（`analysis/segments.ts`）。
  会話量が少ないうちは確からしさが低く出るようにしてある

### 7-3. 人格のDB化とエッジAIへの持ち出し（済）

`src/persona/personaCard.ts`。JSON / システムプロンプト / Ollama Modelfile / 説明書の4形式で出せる。
載せ先のモデルに依存しないよう、記憶は平文、口調は実会話から抜いた応答例で移植する。

### 7-4. 属性・管理画面・マーケット・LP・視線入力（済）

- 属性は選択肢式・全て任意・段階的（`persona/profile.ts`、`/profile`）
- 管理画面は合言葉で保護し、未設定なら開かない（`/admin`）。会話の本文は管理者からも見えない
- マーケットは本人出品と匿名集約セグメントの両方（`/market`）。統計には人数の下限を強制
- toC向けLP（`/lp`）。人格データの売買には触れない方針
- 視線・スイッチによる文字入力（`/eyes`）。8面を3階層たどって1文字、変換はAIに任せる

### 7-5. 残っていること

- **決済**: マーケットは購入希望の連絡を受け取るところまで。金銭のやり取りは未接続。
  値付けより先に「誰に渡してよいか」の運用を決める必要がある
- **視線入力の実機確認**: ウェブカメラの視線推定は個人差が大きい。較正の回数・滞留時間の初期値は、
  実際に使う人に合わせて調整が要る。スイッチ操作へ落ちる経路は用意してある
- **法人向けの統計提供の運用**: 画面はあるが、提供の単位・期間・契約は未定
- ~~**利用規約**~~: 作成済み（`/terms`）。空欄を埋めたうえで弁護士の確認を受けてください

---

## フェーズ8: 訴求と運用の整備（2026-09-13 実装）

- **利用規約**（`/terms`）: 持ち主トークンによる所有権の前提、AI生成物の扱い、マーケット、
  サービス終了時にデータを書き出せる期間を設けること、まで含めた
- **toC LP の作り直し**（`/lp`）: 「分け御霊」という名前の由来を軸に据え、
  文字・声・通話・かざして話す・視線という5つの話しかけ方で分身を形づくっていく、という構成に変更
- **法人向け LP**（`/biz`）: 大規模LLMを常時呼べない現場に向けて、人格を重みではなくデータで持つ構成を訴求。
  問い合わせフォームから直接相談を受けられる（`contact_requests`）
- **覚え書きの書き直し**（`/profile`）: AIが書き留めた内容に間違いがあれば本人が直せる。
  直した直後に蒸留で上書きされないよう、反映済みとして扱う
- **iOS向けのホーム画面追加の案内**: `beforeinstallprompt` 非対応のため、
  iPhone・iPad の Safari でのみ、共有メニューからの手順を表示する

---

## フェーズ9: 復旧と積み残しの解消（2026-09-13 実装）

### 9-1. 分身の復旧（済）

機種変更に失敗して持ち主トークンを失った人を、運営が本人確認のうえで救済できるようにした。

- 利用者側の窓口: `/recover`。届いたものは問い合わせと同じ受け皿に入る（見る場所を増やさない）
- 運営側: 管理画面の「分身の復旧」タブ。タグIDか分身IDで引き、本人確認用の項目を見て、
  理由を書いたうえで引き継ぎコードを発行する
- **復旧に会話の閲覧は要らない**。所有権が戻れば、履歴も記憶も本人が見られる状態に復帰する。
  だから管理画面には中身を出していない。出せる画面を作ると、そこが最大の漏洩経路になる
- 発行は所有権を移す操作なので、必ずログに残す

あわせて、プライバシーポリシーの「運営からも見えない作りにしています」という記述を訂正した。
データを預かっている以上、技術的にはアクセスできる。**仕組みで不可能にしているのではなく、
そうしない運用にしている**という区別を、書き分けるようにした。規約の「復旧のご要望にはお応えできません」も
実態と食い違うので書き換えた。

### 9-2. 積み残しの解消（済）

- signalExtractor: 話題の新規性を、実際に初めて出た語かどうかで測るようにした
- 記憶削除: 消し切るまで繰り返すようにした（200件超で残っていた）
- 出会いの図鑑: 12件ずつのページングに
- `/api/health` に管理画面が有効かどうかを出した（合言葉の設定でつまずいたときの切り分け用）

---

## フェーズ10: ドメイン分離と公開まわりの地固め（2026-09-13 実装）

### 10-1. 2つのドメインに分けた（済）

紹介ページ（`/lp`・`/biz`）を apex（waketama.com）の役割にし、サービス本体は app.waketama.com に寄せた。
同じWorkerが両方を受け、`src/hosts.ts` の `hostRedirect()` が振り分ける。

**本体を1つのホストに寄せる**のが要点。持ち主トークンは localStorage にあり、
localStorage はオリジンごとに別物なので、2つのホストでアプリが開けると
片方で育てた分身がもう片方からは「持ち主ではない」ものに見える。利用者から見れば分身の喪失に等しい。

あわせて、canonical リンク・`robots.txt`・`sitemap.xml` をホストごとに出し分けるようにした。
本体側（app）はクロール全面拒否——個人の分身のページがインデックスされて得をする人はいない。

`wrangler dev` はリクエストのURLもHostヘッダも独自ドメインに書き換えるため、ローカルでは
振り分けを無効にしている（`isEdgeRuntime()`）。これを入れる前は、ローカルで `/lp` を開くと
本番へ飛ばされてLPの確認ができなかった。

### 10-2. 404の受け皿（済）

`public/404.html` を用意し、Workerから返すようにした。

途中でアセット層の `not_found_handling = "404-page"` を使おうとしたが、**これは罠だった**。
有効にすると、アセットに無いパスをアセット層がその場で404にしてしまい、リクエストがWorkerまで
届かなくなる——`/api/*` が丸ごと死ぬ。E2Eが「削除後に一覧へ戻らない」「再タップで分身が発行されない」
という形で拾ってくれた。設定は既定に戻し、wrangler.tomlとREADMEに理由を書き残した。
単体テストにも「404ページが出ること」と「同じ経路でAPIが生きていること」を**必ず一緒に**確認する形で追加した。

### 10-3. 問い合わせの二重化と回数制限（済）

`/lp`・`/biz` のフォームは Formspree に送られる（本人が設定済み）。それだけだとメールが1本残るだけで、
返信済みかどうかを追う場所が無い。送信時に同じ内容の控えを `navigator.sendBeacon` で `/api/contact`
にも送り、管理画面で対応状況まで管理できるようにした。控えの送信が失敗しても本来の送信は妨げない。

あわせて `/api/contact` に1日あたりの回数制限を入れた（`migrations/0006_ip_quota.sql`）。
**IPアドレスは保存しない**。その日限りのランダムな塩を足してハッシュ化した値だけを数え、
古い記録は塩ごとCronで消す。数えるための仕組みであって、身元を持つ必要はない。

### 10-4. Service Workerの緊急停止（済。積み残し 0-6 の解消）

壊れたSWを配ると、こちらが直しても端末側の古いSWが動き続ける。
`SW_KILL=1` を付けてデプロイすると `/sw.js` が解除用スクリプトに差し替わり、
端末側でキャッシュを捨てて自分自身を登録解除するようにした。

```bash
npx wrangler deploy --env="" --var SW_KILL:1   # 緊急停止
```

紹介ドメイン（apex）でも同じものを返している。紹介ページからPWAを入れられると、
起動のたびに本体ドメインへ転送される「入れても意味のないアプリ」がホーム画面に並ぶため。
apex では manifest のリンクもHTMLから外している。

### 10-5. 保護のヘッダ（済）

HTMLに `frame-ancestors 'none'` / `X-Frame-Options: DENY` / `nosniff` /
`Referrer-Policy: strict-origin-when-cross-origin` / `Permissions-Policy` を付けた。
管理画面や引き継ぎ画面を透明なiframeで重ねて踏ませる手口を塞ぐのが主目的。

CSPの本体（スクリプトの制限）はまだ入れていない。全ページがインラインスクリプトで書かれているため、
いま入れると `'unsafe-inline'` を許すことになり防御にならない。nonce を配る作りに変えてからにする。

### 10-6. 管理画面に入れないときの案内（済）

`npm run admin:passcode` / `npm run secrets:list` を用意し、404の画面にも
「引数はシークレットの**名前**であること」「`--env=""` が要る理由」「いま応答している環境と版数」を出すようにした。

### 10-7. 残っていること

- ~~CSP（nonce方式への移行）~~ 実施済み（2026-09-21、10-8）
- ~~事業者名・連絡先・管轄裁判所の空欄~~ 実施済み（2026-09-20、フェーズ5参照）
- apex を custom_domain にするため、既存の A/CNAME レコードがあれば先に削除が必要
  （Cloudflareダッシュボード・DNSレジストラの操作が要るため、私からは実施できない）

### 10-8. CSPをnonce方式に変えた ✅ 実装済み（2026-09-21）

> **2026-09-23 追記**: 実ブラウザで確かめたところ、再訪問時の304・SWの登録・視線入力の後読みの3つで壊れていた。直した経緯はフェーズ23-4。

`frame-ancestors` だけだったCSPに、`script-src 'nonce-...'` を足した。やったことは3つ:

1. `src/index.ts` の `serveAsset()` で、レスポンスごとに暗号乱数のnonceを1つ作る
   （`crypto.getRandomValues`。UUIDを流用しないのは、ハイフンや版数ビットのような
   固定パターンを含めたくないため）
2. 同じ関数内で使っている `HTMLRewriter` に `script` 要素のハンドラを足し、
   **すべての `<script>` タグ**（インラインでも、`cdn.jsdelivr.net` から読む
   model-viewerのような外部srcでも）に同じnonceを `nonce="..."` 属性として振る
3. `content-security-policy` ヘッダの `script-src` に同じnonceを載せる。
   あわせて `object-src 'none'` / `base-uri 'none'` も足した
   （`<object>`/`<embed>`/`<base>` はどこにも使っていないので、閉じても実害が無い）

これで `'unsafe-inline'` を一切許可しないCSPになった。nonceはHTMLRewriterが
全ページ・全`<script>`タグに機械的に振るので、「このページだけ付け忘れた」が起きにくい
（付け忘れたら、そのスクリプトが**動かなくなる**方向に倒れるので、安全側の失敗になる）。

**唯一の例外だった `public/offline.html` の `onclick="location.reload()"`** も直した。
nonceはインラインの `<script>` タグには効くが、`onclick` のようなイベント属性には効かない
（別の仕組み——`'unsafe-hashes'`——が要る）ため、`addEventListener` を使う通常の書き方に変えた。
全ページを見て、他にインラインイベント属性（`onclick`/`onchange`/`onsubmit`等）が
無いことも確認済み（`grep`で全`public/*.html`を走査。これが1件だけだった）。

**検証方法**: `wrangler dev` はAI/Vectorizeのリモートバインディングに
`CLOUDFLARE_API_TOKEN` を要求するため、この環境では起動できず、実ブラウザでのE2E確認はできていない。
代わりに `src/__tests__/index.test.ts` に、①nonceがヘッダに載ること、②`'unsafe-inline'`が
含まれないこと、③HTML中の**すべて**の`<script>`タグにヘッダと同じnonceが振られていること、
④外部src（model-viewer）のタグにもnonceが振られること、⑤リクエストごとにnonceが変わること、
の5点を確認するテストを足した（miniflare上でWorkerの実コードを実行して確認しているので、
「HTMLRewriterの変換が実際に効いているか」までは確認できているが、実ブラウザがこのCSPを
どう解釈するかまでは検証できていない）。公開前に、実機か`npx wrangler dev --remote`が
使える環境で一度、ブラウザのコンソールにCSP違反が出ていないことを確認してください。

---

## フェーズ11: 人格データを「売れる状態」にする（2026-09-14 実装）

すべて **人格データの強化と、販売可能な水準への到達** を目的にしている。

### 11-1. ロゴ（勾玉）を作り直した（済）

サービス名の由来（分け御霊）に合わせて、勾玉そのものを標にした。
作図は3つの円だけで決まる: 大きい円の右半分に頭の円を足し、尾の円を引き、頭に紐穴を開ける。
半径を r1 + r2 = R にしておくと3つの円が同じ縦線で接し、頭から尾へ滑らかに細くなる。

同じ作図から、SVG（`public/icons/mark.svg`／HTMLに直接埋め込み）と
PWAアイコン・OGP画像（`tools/make_brand_assets.py` の `magatama_mask`）を生成している。
LPではメインビジュアルの後ろに大きく透かしで置き、キャラクターが勾玉から出てくるように見せた。
**形を変えるときは、SVG・HTML内のpath・Pythonの3箇所を必ず揃えること。**

### 11-2. UIの穴を塞いだ（済）

実機を模した通しの点検（全ページを実際に開いて、出口と閉じるボタンを数える）を行い、次を直した。

- **`/profile` が持ち主には必ず落ちていた**。`renderNotes()` が自分自身を呼んでおり、
  「Maximum call stack size exceeded」で真っ白になっていた。
  持ち主トークンが無い場合は手前の403で止まるため、**持ち主だけが踏む**壊れ方をしていて、
  普通に開いて確認するだけでは見つからなかった。E2Eも403で止まっていたので気づけなかった
- `/summon` でカメラが使えないとき、案内文が説明文とちょうど重なって両方読めなくなっていた（重ねる配置をやめた）
- 行き止まりの解消: `/summon`・`/call`（開始前）・`/market`・`/recover` に戻り口を置いた
- 全ページのロゴを勾玉に差し替えた

### 11-3. パルスサーベイ（段階別に1問ずつ）（済）

会話の合間に**1問ずつ**聞く仕組みを入れた（`src/persona/survey.ts` / `public/pulse.js`）。

- **属性と価値観を交互に出す**。属性だけでは名簿に近く、価値観だけでは心理テストになる。
  売り物になるのは「30代・首都圏」ではなく「30代・首都圏で、安定より刺激を選ぶ人」のほう
- 価値観の設問は、会話からのAI推定と**同じ8軸**に載る形にした。
  本人が答えた軸は `selfReported` に控え、以降のAI推定はその軸をほとんど動かさない（重み 0.3 → 0.06）。
  聞いておいて次の会話で上書きするのでは、答えた意味がなくなる
- 同意が先。無ければ設問の代わりに同意の依頼を返す
- 「あとで」と「もう聞かないで」を別の操作にした。前者を積み上げて後者と解釈しない

### 11-4. 設問を管理画面から変えられるようにした（済）

`survey_questions`（migration 0007）に**上書きと追加だけ**を持ち、組み込みの設問はコードに残した。

- 表が空でも完全に動く（初期化を忘れて設問が全部消える、という壊れ方をしない）
- 同じIDなら上書き、違えば追加。「既定に戻す」で元の文言へ復帰する
- 出したくない設問は削除ではなく無効化（消すと、次の版で復活したときに気づけない）
- 価値観の設問には選択肢ごとに0〜100の点数が要る。点数の無いものは保存させない
  （答えても何も動かない設問になるため）

### 11-5. 人格データの「厚み」（済）

会話の量（40）・属性（25）・価値観（20）・覚えていること（15）で0〜100。**60以上が出品・提供の目安**。
本人には `/profile`、運営には管理画面の人格データ一覧（`depth_score` 列、migration 0008）に出る。

会話だけでは60に届かないようにしてある。たくさん喋っていても「誰なのか」が分からない分身は、
人格データとしては薄い——この判断を、本人と運営が同じ数字で共有できるようにするのが狙い。

### 11-6. 残っていること

- 設問の追加分は、レジストリ上の厚みでは母数に入れていない（実際より控えめに出る。過大に出すよりは安全側）
- 価値観の設問は1軸1問。同じ軸を複数問で測って安定させるのは、実データを見てから
- 出品の水準（60）は暫定値。実際の出品と引き合いを見て調整する

---

## フェーズ12: 「本当に載せ先で動くのか」を測った（2026-09-14）

「集めたデータでフィジカルAIやメタバースのアバターが動くのか」を確かめるため、
検査を先に書いて測った（`tools/persona-runtime-check.mjs`）。**測ったら、動かなかった。**

### 12-1. 分かったこと（測定結果）

正反対の答えをした2体を作り、書き出した人格カードを測った。

| 指標 | 修正前 | 修正後 |
| --- | --- | --- |
| 到達率（カードの情報がプロンプトに現れる割合） | 55% / 48% | **100% / 100%** |
| 価値観8軸がプロンプトに現れた割合 | **0%** | 100% |
| 属性の到達 | 11項目中2〜3 | 11項目中11 |
| 身体のパラメータ | 無し | あり |
| 応答例の健全性 | **5組すべてがAI失敗時の定型文** | 0件 |

**サーベイで集めた価値観は、JSONの中にはあってもプロンプトには1文字も入っていなかった。**
大半の載せ先は `systemPrompt` しか読まないので、これは「集めたが渡していない」状態だった。
設問を作り込んでも、この1箇所が抜けていれば人格データとしての価値はゼロになる。

応答例の件も実害が大きい。AIが落ちたときの「（今はうまく考えがまとまらないみたい…）」が
そのまま口調の手本として持ち出されていた。載せ先のモデルはこれを真似るので、
**受け取った側には「そういう喋り方の分身」に見える**。

### 12-2. 直したこと

- 属性と価値観をプロンプトに載せた。**数値ではなく行動の指示に翻訳して**渡す
  （「刺激: 82」ではモデルは動かない。「定番より先に、やったことがない選択肢を出す」なら動く）
- 本人の申告と会話からの推定を、プロンプト上でも区別した（買い手にとっては、ここが品質そのもの）
- AI失敗時の定型文を応答例から除外した
- `runtime.avatar` を追加（`src/persona/avatarProfile.ts`）。
  動きの大きさ・応答までの間・目線を保つ長さ・対人距離・姿勢、そして
  価値観から導いた振る舞いの方針（`code` は固定の識別子で、日本語を解さない制御系でも分岐できる）

### 12-3. どこまで確かめられていて、どこからが未確認か

**確かめた（自動で回る）**
- 3つの載せ先（LLM／アバター／フィジカルAI）ごとの必須項目が揃っていること
- 集めた情報が漏れなくプロンプトに届いていること
- 正反対の2体が別人として書き出されること（プロンプトの違い61%、価値観の距離52.9）
- **LLMを1回も呼ばずに**、`policy[].code` だけで4場面中4場面の振る舞いが分かれること

**確かめていない**
- **実際のLLMに載せたときの応答**。クラウド開発環境からはモデルの重みを取得できない
  （配信元が許可リストの外）ため、ここは手元で実行する形にした:
  `PERSONA_LLM_URL=http://localhost:11434/v1/chat/completions node tools/persona-runtime-check.mjs`
- 実機のロボット・メタバース上での見え方。数値の対応づけ（`avatarProfile.ts`）は初期値であり、
  実機を見ながら調整する前提
- 小さいモデル（3B以下）で、この長さのプロンプトが破綻しないか。
  プロンプトは約2,000文字あり、載せ先によっては削る必要が出るかもしれない

---

## フェーズ13: 育ちの実感と、入口の整理（2026-09-19 実装）

### 13-1. 成長段階を4→8段に ✅

50回で最上位に着いてしまい、そこから先は何回話しても表示が変わらなかった。
1日に数回話す人なら2週間で打ち止めになる計算で、育てること自体が体験のサービスとしては致命的。

- 段階の間隔を後ろほど広げた（5 → 15 → 35 → 70 → 130 → 220 → 360）
- 段階に意味を持たせた。文脈に載せる会話数・思い出す件数・返事の長さが段階ごとに変わる
- 定義元を `src/ai/growth.ts` 1か所に集約（以前は段階判定と文脈量の閾値が二重に書かれていた）
- チャット画面に進み具合の帯と「あと○回で『××』」を出した

### 13-2. 前回までの会話が画面に戻る ✅

「残さないモードでもないのに履歴が出ない」状態だった。保持数も24発言しかなかった。

- `CharacterState.getDialogue()`（持ち主のみ）と `GET /api/character/history` を追加
- 保持を60発言に拡張。画面には区切り線（「ここまでが前回まで」）を出す

### 13-3. 覚え書きに「本人が書く土台」を足した ✅

会話からの自動学習だけだと、何十ターン話すまで覚え書きが空のままだった。

- `profileNotesSeed`（本人が書く／AIは書き換えない）と `profileNotes`（会話から）を別枠に
- 使うときは必ず `mergeNotes()` で1つにまとめる。片方だけ渡す場所を作ると
  「プロフィールに書いたのに会話では知らない」が起きる
- `/profile` を2段の画面に。書くことに詰まらないよう、ひな形を入れるボタンを置いた
- `POST /api/notes` に `part: "seed" | "learned"`（既定は learned＝既存クライアント互換）

### 13-4. 入口を「依代（よりしろ）」に統一し、QRを加えた ✅

画面の文言が「NFCタグ」固定で、QRで来た人に嘘の案内が出る状態だった。

- 画面の言い方を依代に統一（規約・プライバシーポリシーは定義として1度だけNFCに触れる）
- `/q/:code` を追加。**`/t/:code` と完全に同じ処理**にした
  - 1つの依代から1体だけ／姿はランダム／確率は入口によらず等しい
  - `CharacterState.init()` に見た目を渡す引数は**意図的に持たせていない**
- 「その場所でしか手に入らない」は、姿を固定するのではなく配る場所で作る（`spots`）
- 依代を持たない人の入口 `/add` と `POST /api/character/new`（1日3体まで）
- 管理画面に「依代（タグ・QR）」タブ。発行・CSV書き出し・QR画像の生成まで
- 台帳: `tag_registry` / `spots` / `character_origin`（migration 0009）

### 13-5. 実機で人格データが動くことの確かめ方 ✅

- `tools/edge/compact.mjs`: 人格カード → **数値と識別子だけの数百バイト**（実測 240〜250B）
  - 会話・覚え書き・属性が混ざっていないことを機械で確認してから書き出す
- `tools/edge/rule_runtime.py`: LLMを使わない参照実装。性格の違う2体で
  返すまでの間が 410ms / 930ms、身振りが 2回 / 1回、近づくか否かが分かれることを確認した
- ESP32（Arduino）と Pico（MicroPython）の実装も同梱
- 手順書: `docs/EDGE_DEVICE_TEST.md`（機種ごとの線引き・合格基準・記録票つき）

### 13-6. 「実際に何を売るのか」を決めて実装した ✅

売るのは分身そのものではなく、**その人格を相手の環境で動かせる形にした写し**と、
それを取り出す権利（引換券）。所有権は持ち主のまま動かない。

- 商品は4つ（`src/delivery.ts` の `SKUS` が唯一の定義元。管理画面はそれを表示するだけ）
  - 人格カード / 振る舞いプロファイル（**LLM不要**）/ MCP接続 / 持ち出し一式
- 引換券 `delivery_grants`（migration 0010）: 1体に紐づき、範囲と期限を持つ。
  平文は発行時の1回だけ（保存はハッシュ）。持ち主トークンは買い手に一切渡さない
- 買い手の口 `GET /api/delivery?scope=...&token=...`
- **MCP** `POST /mcp`（`src/mcp.ts`）: JSON-RPC 2.0 over HTTP。読み取りだけで、
  書き込みの道具は作らない。`persona_reply` は保存しない経路なので、
  **買い手が何度話しかけても持ち主の分身は育たない**（テストで担保）
- 管理画面に「納品」タブ。商品一覧・引換券の発行/失効・先方へ送る手順の生成まで
- 書類: `docs/SALES_DELIVERY.md`

### 13-7. 運用マニュアル ✅

`docs/OPERATIONS.md`。毎日/毎週/毎月に見るもの、復旧の手順、依代の配り方、
障害の切り分け、やってはいけないこと（理由つき）。

### 13-8. NFCキーホルダー（50mm角・削り出し）✅

`docs/KEYHOLDER_DESIGN.md` と `docs/keyholder/`（図面SVG 2枚＋DXF）。
図面は `tools/keyholder/make_keyholder.py` が**ロゴと同じ作図から生成**する
（図面だけ手で描くと、ロゴを直したときに現物だけ古い形で残るため）。

設計上の最大の論点は「**金属はNFCを止める**」こと。削り出しとNFCは素のままでは
両立しないので、樹脂で作るか、金属＋フェライトシートにするかを先に決める形にした。

### 積み残し

- キーホルダーの試作と、実機での読み取り確認（フェーズ15でエッジ訴求の追記は済）

---

## フェーズ14: 売り方の言葉を直し、出品を畳み、画面の穴を塞いだ（2026-09-19 実装）

### 14-1. 「自分専用のSLM」と、判断特化AIへの受け渡し ✅

**用語の判断から。**
- **SML は誤り。正しくは SLM**（Small Language Model）。SML は Standard ML という
  プログラミング言語の略称として通っているので、資料に出すと別物にぶつかる
- **Jev は実在する他社製品**（TypeSafe AI、2026年9月発表）。文章を生成せず、
  Choice / Score / Noul の3種類の問いに確率つきの値だけを返す判断特化モデル。
  重みも学習方法も非公開、APIのみ。したがって**「自分専用のJevを作れる」とは言えない**
  （作れないし、他社製品名を自社の生成物として名乗ることになる）。
  しかも Jev は喋らないので、「喋って作る」という体験とも噛み合わない

**そのうえで、相性は良い。** わけたまが会話から取り出しているもの（性格6軸・価値観・方針コード）は、
そのまま「この状況でどちらを選ぶか」という判断の癖で、判断特化モデルが欲しがる入力そのもの。
立ち位置を「Jevを作る」から「**判断特化AIに渡せる形で書き出す**」へ変えた。

- `src/slm.ts`: SLM一式（Modelfile + LoRA学習データ + 手順書）と判断プロファイル
- 納品物に `slm` / `decision` を追加。管理画面の「納品」タブに**下見**も置いた
  （引換券を出さずに、実際に渡るものをその場で見せられる）
- 文言の取り違えはテストで縛った（SMLと書いたら落ちる／Jevを「作れる」と書いたら落ちる）

### 14-2. 出品機能の廃止と、はじめる前の同意 ✅

- **利用者どうしのマーケット（出品・購入申し込み）を削除。**
  人格が他人の手に渡る経路を開けたままにすると、「誰に渡ってよいか」の運用を固める前に
  取り返しのつかない事故が起きうる。企業提供は引換券に一本化した
  （`src/market.ts` → 集約統計だけを `src/insights.ts` へ。`/market` ページとAPI、
  購入希望タブ、`getListingSummary` を削除）
- 同意を作り直した（`CONSENT_VERSION` 2）。`marketplace` を廃止し、**`terms` を追加**
  - 個人情報を預からないこと、**運営が改善や匿名提供に使うことがある**ことへの同意
  - **同意するまで会話を始めさせない**（`public/gate.js`。/summon と /chat の両方に差す）
  - 版を上げたので、既存の同意は取り直しになる（文面が変わったのに流用しない）
- 利用規約・プライバシーポリシーを全面的に書き直し

### 14-3. 会話画面と「これ見て」の修正 ✅

- **キャラクターが常に見えるようにした。** 「たたむ」で高さ0まで消せたため、
  会話が伸びると話し相手が画面から居なくなっていた。たたむ＝小さくする（消えない）に変更し、
  スクロールしても上に残るようにした
- **3Dが描けない端末のための2D差し替え。** WebGLが無い環境では model-viewer は例外も投げずに
  何も描かず、キャラクターの居ない会話画面になっていた。6秒待って読み込めていなければ2Dの絵に切り替える
- **過去の会話は畳んで表示。** 開いた瞬間に古いやり取りで埋まらないよう、直近4件だけを出し、
  残りは「これまでの会話を見る（N件）」の下に入れた
- **「これ見て」が効かなかったのを修正。** 予約ボタン（次に話しかけたときに画像を添える）だったため、
  カメラに何か映して押しても、声を出すまで何も起きなかった。**押した瞬間に送る**形に変更し、
  カメラが使えないときは理由を出すようにした

### 14-4. 分身ごとの声 ✅

種族（5）× 色（6）× 個体 × 性格の4階建てに作り直した。
以前は characterId のハッシュだけで決めていたうえ、会話画面はその値を使わず
その場で性格から計算していたため、**種族も色も違う子が全員ほぼ同じ声で喋っていた**。

- 30通りの組み合わせがすべて違う値になることをテストで担保
- 全体に高め・軽めへ寄せた（下限 1.18）。手のひらサイズの見た目に、低い声は合わない
- 会話画面は、サーバーが決めた値を当てるだけにした（決め方を2箇所に持たない）

### 14-5. テキスト → ロボットの行動 ✅

`docs/TEXT_TO_BEHAVIOR.md`。6段の変換過程、実測値、**限界**（言葉しか観測していない、
対応づけは初期値、会話が浅いと差が出ない）まで書いた。
/lp・/biz・運用マニュアル・納品書類・実機検証手順からも参照している。

### 14-6. サービス紹介動画 ✅

`tools/lp-video/make_video.mjs`。**実際に動かしたサービスを撮って作る。**
画面・キャラクター・成長段階・厚みはすべて本物で、会話の文面だけが紹介用。
作り方をコードにしてあるのは、画面を直したのに動画だけ古い版のまま残るのを防ぐため。

1280×720 / 約26秒 / 1.5MB、`preload="none"` で /lp に設置。

**この動画を作る過程で、`/api/character/history` を新旧2つの用途で取り合っていて、
成長ページが静かに空になっていたことが見つかった**（会話履歴を `/api/character/dialogue` へ改名）。
実画面を撮る作業そのものが、通しの確認として効いた。

---

## フェーズ15: エッジ／スタンドアロン訴求と、実画面の掲載（2026-09-19 実装）

フェーズ13の積み残し「`/lp`・`/biz` へのエッジ／スタンドアロン訴求（実データ・実画面で）」を解消した。

### 15-1. 紹介ページに載せる数字を、測り直した ✅

文書と紹介ページに載っていた実測値（240〜250バイト、410ms / 930ms、±10.8度 / ±2.3度）が
**古くなっていた**。検査用の2体は `npm run persona:fixtures` が毎回その場で作り直しており、
性格の抽出をAIが行うので、同じ回答を入れても値が動く。

測り直した値（2026-09-19）に差し替えたうえで、**「実行のたびに数ポイント動く。差の向きは変わらない」
と併記した**。この断りが無いと、相手が再現したときに数字が合わず、仕組みごと疑われる。

| | さきがけ | ひだまり |
|---|---|---|
| 書き出しの大きさ | 256バイト | 259バイト |
| 返すまでの間 | 458ms | 890ms |
| 待機の揺らぎ | ±10.2度 | ±3.6度 |
| 心地よい距離 | 1.3m | 1.4m |

差し替え先: `docs/TEXT_TO_BEHAVIOR.md`、`docs/SALES_DELIVERY.md`、`docs/OPERATIONS.md`、
`public/lp.html`、`public/biz.html`。再現手順（3コマンド）も文書に書いた。

### 15-2. `/biz` にエッジ向けの節を足した ✅

`#edge`。「動く」の3段階（振る舞い／言葉／常時オフライン）、機種ごとの線引き
（Pi 5 〜 ESP32 の6機種・段階Aは全機種で成立）、**実際の書き出し256バイトの中身**、
参照実装 `rule_runtime.py --diff` の出力、そして限界。

言いたいことは1つ。**喋ることと、その人らしく振る舞うことは別の問題**で、
後者は数値の分岐で足りる。Pico や ESP32 に小さなLLMを載せるのは勧めない、とも明記した。

### 15-3. 実画面の写真を載せた（モックではない）✅

`tools/lp-video/make_shots.mjs`。撮影用の分身と撮り方は紹介動画と共有しており
（`tools/lp-video/demo.mjs` に切り出した）、**動画の中の子とLPに載っている子が別人にならない**。
`public/media/shots/*.webp`（4枚・合計約120KB）。画面を直したら1コマンドで撮り直す。

### 15-4. 撮っている最中に見つかった不具合 ✅

- **成長グラフが、読み込み直後は必ず空だった。** `hidden` のまま `drawChart()` を呼んでおり、
  親要素の `clientWidth` が0。幅0の領域へ描いていた。画面を回すと直るので気づきにくい。
  表示してから描くように直し、E2Eでキャンバスの塗られた画素数を数えて毎回押さえるようにした
- 記録が1回ぶんしか無い分身では、線が引けず白紙に見えていた。グラフごと出さずに理由を書く
- **`/biz` のロゴだけ古い簡略版の勾玉だった**（他14ページは新しい形）。個人向けと法人向けで
  ロゴの形が違っていた。揃えて、E2Eの確認対象に `/biz` を足した
- 撮影用の会話に英単語が1つ紛れていた（「today の分だけでも」）

---

## フェーズ16: 依代を売る／公開前の積み残し（2026-09-19 実装）

### 16-1. 専用NFCキーホルダーの販売案内 ✅

`/lp` に `#product`。**5,500円（税込・送料別）の買い切り**、購入はお問い合わせから。
本サービス上に決済は置かない（置くと特商法まわりの話が一気に増える。いまの規模では
個別対応のほうが速いし、誤解も少ない）。

- 商品写真は `public/media/keyholder.jpg`。**いま入っているのは設計データから起こした仮画像。**
  現物を撮ったら同じ名前で上書きするだけでよい（ページ側は触らない。比率4:3・300KB以内）
- 「購入する」を押すと問い合わせフォームへ飛び、**用件が先に入っている**。
  白紙の入力欄の前で止まる人を減らすため。ただし**書きかけがあるときは上書きしない**

**売り物ができたことで、書類のほうにも穴が開いた**（ここが本題）。

- 規約 §4「料金」: 「料金はかかりません」のままだった。分身は無料・依代は有償と書き分け、
  決済を置いていないこと、売買契約がいつ成立するかを明記
- ポリシー §1「取得しない情報」: **氏名・住所を取得しないと書いてあった。**
  物を送る以上これは嘘になる。§1-2 を足して、何を・何のために・いつまで持つかを表にした。
  カード番号は預からないこと、**分身のデータとは結びつけない**ことも明記
- LPのFAQも同じ理由で直した（3箇所が食い違うと、どれが本当か分からなくなる）

### 16-2. 音声・カメラの扱いと、ホーム画面への追加 ✅

- 規約に §6-2「音声とカメラを使う機能について」を追加。音声も映像も保存しないこと、
  **周りに人がいる場所では了解を取ってから使うこと**（§5 の禁止事項にも1行足した）
- ホーム画面への追加の案内を `public/install-hint.js` に**1本化**した。
  以前は `/home` にしか無く、**依代から来てそのまま会話画面に居続ける人には一度も出ていなかった**。
  依代を無くしたときに戻れなくなるのは、まさにその人たち
  - iOS Safari には共有メニューの手順を文章で、Chrome系にはボタンを出す
  - **入れられない端末には何も出さない。** Chrome for iOS は共有メニューの項目が違うので除外
  - 会話画面では4往復してから出す（名前を付けた直後に案内が出るのはうるさい）
  - ホームでは1体も居ないうちは出さない（戻ってくる先が無い）
- `npm run legal:check`: 規約とポリシーに残った空欄を数える。
  **空欄が残っているのに「下書きです」の但し書きが消えている**（逆も）と落ちる

### 16-3. 性格更新のチューニング ✅

**いちばん効いたのは、端への張り付きを止めたこと。**

これまでは「positive なら温かさ +1.5」と一律に足していた。ふつうに可愛がっていると
**35回ほどで100に達し、そこから先は何を話しても動かない**。長く育てた分身ほど全部の軸が
振り切れて、**育てるほど他の子と見分けが付かなくなっていた**。育成サービスとして逆立ちしている。

残りの幅で割り引くようにした（`nudge`）。50のときは従来と同じ、端に近いほど効かず、
端からは倍の速さで戻る。0と100には漸近するだけで到達しない。

放置の扱いも直した。以前は再会した1回で陽気さが最大 −8。可愛がり1往復が +1.2 なので、
**2週間の旅行から帰ってきただけで7往復ぶんが吹き飛んでいた**。出張や入院で離れた人が、
戻ってきた初回にいちばん冷たい反応を受けることになる。**寂しがるのと、罰するのは別。**
1回あたりの打撃を大きく下げ、上限も10日ぶんから7日ぶんにした。

`src/ai/__tests__/personality.test.ts` に、この性質そのものを確かめる検査を6つ足した
（500往復しても100に張り付かない／性格が逆の2体は300往復後も40点以上離れている、など）。

---

## フェーズ17: 入口の迷いを消し、場所限定の姿を作れるようにした（2026-09-19 実装）

### 17-1. 「タグに何を書き込めばいいのか」を、迷う場所に置いた ✅

答えは `https://app.waketama.com/t/<コード>`（QRに刷るなら `/q/`）の1本だけ。
文書（OPERATIONS.md §5）には書いてあったが、**作業する人が見るのは管理画面**なので、
依代タブの先頭に実ドメイン入りのURLとコピーボタンを置いた。よくある間違いも表にした。

- **`/home` を書き込むと、かざしても分身が生まれない。** あれは「その端末で既に会った分身の置き場」。
  紛らわしいのは確かなので、管理画面に名指しで「書き込まないでください」と書いた
- 依代を使わない入口は `/add`（その端末だけで1体。1日3体まで、依代には紐づかない）

### 17-2. 何体でも育てられることを、はっきりさせた ✅

**もともと上限は無い**（依代3つで3体）。「1つにつき1体」は依代の話で、人の話ではない。
そこが混ざって読めたので `/add` と OPERATIONS.md の書き方を直し、
検査で固定した（vitest 2件・E2E 3件）。

### 17-3. 場所限定の姿 ✅

これまで「運営が姿を決められる仕組みは置かない」としてきた（0009）。理由は変わっていない——
運営が個体を決められると、手に入れた喜びが「引き当てた」ではなく「配られた」になる。

そのうえで場所限定の姿を作れるようにした。両立のために、**決められる範囲を割った**。

| 決められる | 決められない |
|---|---|
| **どの範囲から引くか**（この神社のQRからは「きらつぶ」しか出ない） | **その範囲の中で、どの子が出るか** |

`init()` に渡せるのは候補の集合（`BirthPool`）までで、引くのは常にDO側の乱数。
1つに絞れば結果は指定と同じになるが、それは「その姿しか居ない場所を作った」ということで、
抽選をすり抜けたわけではない。管理画面でもそう説明し、1種類だけ選ぶのは勧めていない。

- migration 0011: `spots.species_pool` / `color_pool`。空なら制限なし（＝ほとんどの配布元）
- **知らないキーは保存時に捨てる。** 綴り違いを黙って保存すると、「絞ったつもりが全体から引かれている」
  という、現物を配ってからでないと気づけない事故になる
- **全部選んだら「制限なし」と同じ扱い**にして、限定フラグを立てない
- **止めた配布元（停止中）の限定は効かせない。** 止めたはずの企画が配り残しのコードから生き続けない
- 利用者の画面には「ここでしか出会えない姿です」とだけ出る。
  **何が出るかはAPIも返していない**（先に見えると、引く意味が消える）

### 17-4. 集めた姿の図鑑（`/home`）✅

限定の姿を作っても、**「あと何が残っているか」が見える場所が無ければ集める遊びにならない。**
5種族×6色＝30枠を並べ、会ったものだけ開く。まだの枠は `?` で、形も色も出さない。

出せるのは**その端末で会ったことのある分身だけ**（アカウントが無いため）。その断りも画面に出した。

---

## フェーズ18: 全タグに同じURLを書いても「1枚1体」が成立するようにした（2026-09-19 実装）

### 18-1. 何が問題だったか

フェーズ17まで、依代は `/t/<コード>` の**1枚ずつ違うURL**だった。数十枚なら回るが、
**キーホルダーを数百枚単位で外注して売る**となると、1枚ずつ書き込み内容を変える工程が要る。
そこが詰まると商品にならない。かといって全部を同じURLにすると、
サーバーからは1枚も見分けが付かず「1つの依代から1体」が崩れる。

### 18-2. UIDミラーで両立させた ✅

NTAG213/215/216 の **UID ASCIIミラー**を使う。読み取られた瞬間に、
**チップが自分の固有番号（UID・14文字）をURLの中の埋め草へ差し替えて**返す。

```
書き込む内容 : https://app.waketama.com/t?u=00000000000000   （全枚同じ＝外注できる）
1枚目が返す  : https://app.waketama.com/t?u=04a1b2c3d4e5f6   （タグごとに違う＝1枚1体）
```

UIDは工場で焼かれていて書き換えられないので、こちらで採番する必要もない。
NDEFの中身として読まれるため、**iPhoneでもAndroidでも効く**（Web NFC のように
ブラウザの対応に左右されない）。`src/yorishiro.ts` の `identifyTag`。

- `/t?u=<UID>` と `/t?u=<UID>x<カウンタ>` を受ける。カウンタミラーを併用しても、
  **読むたびに別の子にならない**（区切りより後ろは捨てる）
- 大文字・区切り記号（`04:a1:b2:...`）も同じ1枚として扱う
- `/t/<コード>` の従来方式はそのまま使える。QRは紙なので今後もこちら

### 18-3. いちばん怖い事故を、先に潰した ✅

**ミラーの設定を忘れて出荷すると、全部のタグが埋め草のまま同じURLを返す。**
そのままなら**買った人全員が同じ分身を共有する**。気づくのは苦情が来てから。

- 埋め草らしいUID（全部0・全部F・長さ違い・16進以外）は**受け付けない**
- 受け付けなかったときは `yorishiro.uid_filler` としてログに残す（切り分け用）
- 管理マニュアルと管理画面に「**納品されたら必ず2枚かざす**」と書いた。
  1枚しか試さないと、この事故は見抜けない

### 18-4. 見分けが付かなかったときの受け皿 ✅

`/summon?claim=tag`。**かざした人の前で「何も起きない」にはしない。**
こちらの書き込み設定の都合が、そのまま出会いの失敗になるのは筋が悪い。
端末ごとに1体を割り当て、2回目からは同じ子に戻す。

割り切っていること: その状態では**依代を何枚持っていても1体**になる。
だから受け皿は逃げ道であって、**1枚1体を成立させるにはミラーが要る**。

### 18-5. 共通URLで失われるもの（正直に）✅

タグが台帳に載らないので、次の3つは1枚ずつコードを振る方式でしか使えない。

| | 共通URL（UIDミラー） | 1枚ずつコード |
|---|---|---|
| 書き込み | 全枚同じ。外注できる | 1枚ずつ違う |
| 未使用の在庫 | **台帳では分からない** | 分かる |
| 配布元の記録 | 付かない | 付く |
| **限定の姿（17-3）** | **使えない** | 使える |

**場所限定の企画はQRで行う。** 管理画面にもそう書いた。

### 18-6. 依代を使わず、Webだけで始める（`/w`）✅

**タグが刷り上がる前でもサービスを出せるように、リンク1本で始められる口を作った。**
配る・QRに刷る・SNSに貼る、どれでもよい。開いた瞬間に1体生まれ、
同じ端末で2回目に開くと同じ子に戻る（**開くたびに増えない**——ここが壊れると数がすぐ意味を失う）。

受け皿（18-4）をそのまま使っている。違うのは記録だけで、入口の種類を `web` として数える。
`direct`（`/add` のボタンを自分で押した人）と分けてあるので、
**配ったリンクがどれだけ効いたかが別に見られる。**

| やりたいこと | 使うURL |
|---|---|
| キーホルダーを売る | `/t?u=00000000000000`（全枚共通） |
| 場所限定の企画をやる | `/q/<コード>`（1枚ずつ発行） |
| **タグ無しで、いますぐ始める** | **`/w`** |
| 自分の端末でもう1体作る | `/add` |

---

## フェーズ19: はじめての流れを組み直した（2026-09-20 実装）

### 19-1. 同意ボタンが、最初の画面で見えていなかった 🐛

**この節でいちばん重いのはこれ。**

同意の画面（`gate.js`）は差し込み先のページのCSSの中で描かれる。召喚画面には
登場演出のための `button { opacity: 0 }` があり、それが**同意ボタンにも効いていた**。
押せてはいたのでE2Eは通っていたが、**人の目には文章の壁だけが見えて、
進むところが無い**状態だった。生まれてすぐの人が、そこで止まる。

`.wtGate button` に `opacity: 1` を明示。あわせて、
**ボタンの見え方をE2Eで見るようにした**（`getComputedStyle().opacity`）。
押せるかどうかだけを見ていると、この種は二度と捕まらない。

以降、差し込みで描くものは「親の指定が来ても壊れない」書き方にする。

### 19-2. 任意の同意も、はじめに全部見せる ✅

以前は必須の `terms` だけを聞き、`profile` / `aggregate` は設定画面に置いていた。
そこまで見に行く人はほとんど居ないので、**何に使われるのか知らないまま使い続ける**
ことになっていた。後から聞かれて初めて知る形は、同意を取ったとは言いにくい。

3つとも最初に出して、選んでもらう形にした。ただし**既定はオフのまま**。
まとめて出すのは説明を尽くすためで、ついでにオンにしてもらうためではない。
**初期値をオンにした時点で、これは同意ではなく黙認になる。**

文面が長くなったので、押すところは下に貼り付けた（`position: sticky`）。

### 19-3. 名前のつぎに「土台」を聞く ✅

分身が覚えていることは会話から少しずつ溜まるが、**溜まるまでの最初の数十回は
相手を何も知らないまま話す**。そこがいちばん「話が通じない」と感じるところで、
離脱もそこで起きる。3行でも先に渡しておくと、1回目から噛み合う。

- **なぜ書くのかを、その場に出す。** 書く理由の分からない入力欄は飛ばされる
- **必須にしない。**「あとで書く」を同じ大きさで並べた。生まれた直後に長文を
  求めると、そこで止まる。ひな形も置いた
- 書いたものは `/profile` の「1. あなたが書く土台」と同じ場所に入る。
  会話から覚えたことは、その下に積み上がる

この段だけ舞台の縦横比（3/4）を外して伸ばしている。そのままだと入力欄が
画面の下に押し出され、書く前にスクロールが要る（実際にそうなっていた）。

### 19-4. 依代を持たない人の入口を、既定で閉じた ✅

依代は売り物（5,500円）なのに、その横に「この端末で分身を始める」ボタンが
**誰にでも押せる形**で置いてあった。買う理由が消えるうえ、押されただけDOが増える。

かといって完全に塞ぐと、**タグのUIDミラーが効いていなかった人**（＝ちゃんと買った人）
まで締め出す。そこを分けた（`src/entryPolicy.ts`）。

| | 作れるか |
|---|---|
| 依代をかざした・読み取った | **いつでも。** 設定に関係なく通す |
| その端末で一度でも依代を読んだ人 | 作れる（90日。2枚目を買った人を締め出さないため） |
| 管理者 | いつでも |
| それ以外（`/add` の3番・`/w`） | **作れない。**「依代をお持ちの方だけ」と案内する |

開けるのは `--var ENTRY_OPEN:1`。タグが刷り上がる前の時期は立てて、売り始めたら外す。

- **押せないボタンを出さない。** `/add` はサーバーに聞いてから3番を出す
  （押してから断られると、「壊れている」と「そういう決まり」の区別が付かない）
- 閉じているとき `/w` は `/add?closed=1` へ送る。黙って何も起きない、にしない
- **これは鍵ではなく扉。** 判定はCookieなので curl なら偽れる。防ぎたいのは
  「画面にボタンがあるから押される」であって本気の相手ではない。
  機械的な量産は、回線ごとの1日3体で止めている——と、そう書いておく

### 19-5. 管理者の判定まわりを整えた ✅

19-4 で「管理者なら作れる」を足したので、**管理者になる・やめるの筋道**をはっきりさせた。

- **なり方は1つだけ。** そのブラウザで `/admin?key=<合言葉>` を開く。
  合言葉はCookie（`waketama_admin`, `Path=/`）へ移り、URLからは消える。
  判定も1箇所（`src/admin.ts` の `isAdminRequest()`）
- **12時間 → 30日。** 運用する端末は自分の手元の1台なのに、ほぼ毎日入れ直すことになっていた。
  出先でキーホルダーの読み取りを試している最中に切れるのがいちばん困る
- **降りる口を足した**（`/admin?logout=1`。管理画面の右上にも導線）。
  これが無いと、人に貸した端末を戻す手立てが「合言葉を変える」＝**全端末が落ちる**しかない

### 19-6. 分身が生まれる入口が、apex で開けてしまっていた 🐛

`hosts.ts` は「アプリ本体を2つのホストで開かせない」ためにあるのに、
**`/add`・`/t`（共通URL）・`/q`・`/w` が転送の対象から漏れていた。**
`/t/<コード>` だけが入っていて、あとから足した入口が追随していなかった。

localStorage はオリジンごとに別物なので、apex で分身が生まれると
**持ち主の印が apex 側に保存され、本体ドメインからは二度と見えない**。
育てた本人が持ち主でなくなる（本人には「消えた」としか見えない）。
しかも**URLを刷ってから気づいても、配った現物は直せない**。

4つとも転送対象に足し、クエリ（UIDや引換券）を落とさないことも含めて検査で固定した。
`APP_ONLY_PATHS` には「入口を足したらここにも足す」と書いた。

### 19-7. 1日の上限を、通り方ごとに変えた ✅

「1体も作っていないのに『今日はもう3体つくりました』と言われる」という報告から。

原因は2つ重なっていた。

1. **上限（1日3体）が、誰に対しても同じだった。** もともとは「誰でも押せるボタン」を
   守るための数字。19-4 で入口を閉じたのに、数字だけが残っていた
2. **ボタンを押していなくても数が減る。** `/w` を開く、ミラーの効いていないタグを
   かざす——どちらも画面上は何も押していないが、中では1体作っている。
   試している当人には「作った覚えがない」としか見えない

| 通り方 | 1日の上限 |
|---|---|
| 管理者 | **なし。** 試作の読み取り確認では1日に何体も作る。**運営が自分の道具で詰まるのがいちばん無駄** |
| 依代を持っている | 10体 |
| `ENTRY_OPEN` で誰でも入れる状態 | 3体 |

あわせて、断るときの文面に**いつ戻るか**を入れた。
数えているのはUTCの1日なので、**戻るのは日本時間の朝9時**。
「夜中を回ったのに戻らない」と言われる前に書いておく。

### 19-8. 同意を1つにまとめた（版 2 → 3）✅

入口で3つ聞いていたものを、**「はじめてよいか」の1つだけ**にした。
同意すると「あなたのことを分身に覚えさせる」「統計として使うことを許可する」も一緒に有効になる
（`BUNDLED_WITH_TERMS`）。

生まれた直後の人に3つの判断を求める形になっていて、結局ほとんど読まれずに素通りしていた。
選ばせることより、**使い道を全部見せてから、始めるかどうかだけを決めてもらう**方が実際に伝わる。

束ねる代わりに、次の2つは外さないことにした。**どちらか欠けると、ただの黙認になる。**

1. 入口の画面に、束ねた使い道を**チェックではなく本文として1つ残らず出す**（畳まない・省略しない）
2. あとから**1つずつ止められる**（`/profile` の「データの使い道」。止めた分のデータはその場で削除）

| 守ること | どこで担保しているか |
|---|---|
| 止めた用途が、次回の同意確認で黙って戻らない | `normalizeConsent()` は、いまの版の記録があればそちらを優先。テストあり |
| 版 2 の同意を流用しない | `CONSENT_VERSION = 3`。範囲が広がったので取り直しになる |
| 画面が勝手に用途を足さない | `gate.js` は `{terms:true}` だけ送り、束ねるのはサーバー1箇所 |

規約とプライバシーポリシーの記述も直した（「初期状態はオフ」→「同意に含まれ、あとから止められる」）。

---

## フェーズ20: 実機の検証機を設計した（2026-09-20 実装）

「ラズパイ4／ゼロ／Pico／ESP32 で検証用デバイスを作りたい。設計書・仕様書・コードを全部」から。

### 20-1. 何を証明する機械にしたか ✅

1台動かしても「人格がクラウドの外でも通用する」ことは示せない。示せる条件を3つに決めた。

1. 同じ配線・同じファームのまま、**人格を差し替えると動きが変わる**
2. 性格が逆の2体に、**同じ瞬間に同じイベントを投げると、違う動きが出る**
3. **ネットを切っても 1 と 2 が続く**

2 を満たすために、この検証機の中心は「2台同時」と「自動判定」になった。

### 20-2. 4構成 ✅

| 型番 | 石 | 位置づけ | 部品代の目安 |
|---|---|---|---|
| WT-1 たね | Pico 2 W | 単体で完結。いちばん小さい証明 | 約3,000円 |
| WT-2 ふたば | ESP32 | Wi-Fi で人格を受け取る／差し替えを見せる | 約3,500円 |
| WT-3 こだち | Pi Zero 2 W | Python のまま＋記録 | 約5,000円 |
| WT-4 もり | Pi 4 ＋ 上記2台 | 2体同時比較・記録・段階B（Ollama） | 約22,000円 |

機種ごとに作り込まない。**同じ体・同じ顔**にしないと、差が人格から来ていると言えなくなる。

### 20-3. 写しがずれないようにした ✅

同じ規則が4箇所に書かれている（Python・C++・参照実装・最小形の変換）。
ずれると**機種によって性格が変わり**、現場では「ESP32だけ挙動が違う」としか現れない。
人の目で見比べるのをやめて、`npm run device:selftest` が7項目を機械で確かめる。

- `wt_core.py` ↔ `rule_runtime.py`（4イベント×2体）
- `wt_core.py` ↔ `wt_core.h`（**g++ でホストにビルドして実行**。5イベント×2体）
- `wt_compact.py` ↔ `compact.mjs`（**1バイトまで一致**）
- ピンの重複・ESP32の禁止ピン・生成物が `pinmap.json` と一致しているか

配線図とファームのピン定数は `pinmap.json` から生成する（`npm run device:docs`）。
手で3箇所直すと必ず1つ残り、その1つを頼りに配線した人が半日溶かす。

### 20-4. 境界を機械で守る ✅

```
わけたま → 母艦（トークンを持つ唯一の場所）→ ★ → 子機（数値しか無い）
```

★ を越えるのは約250バイトの最小形だけ。`audit()` が会話・覚え書き・属性・systemPrompt の
混入を見つけたら**配布を止める**。子機のファームには、トークンを入れる場所そのものが無い。

### 20-5. 「2倍以上」という基準が、実測と食い違っていた 🐛

`EDGE_DEVICE_TEST.md` の合格基準「返すまでの間が2倍以上」は、ある1回の実測（410 vs 930）を
そのまま線にしたもの。ところが同じ文書の別の実測（458 vs 890）は **1.94倍で不合格**になる。
判定を実装して初めて気づいた。**1.5倍**に直し、理由を残した。

### 20-6. 機材が無くても通せるようにした ✅

`npm run device:demo` で、同じPCの上に子機を2つ立てて操作盤を出す。
見本の人格2体（`tools/device/samples/`）が同梱してあるので、**分身も機材も無い状態で**
2体同時のイベント送出・差の判定・記録CSVまで確認できる。
実測もちゃんと出る（見本の2体で 459ms vs 890ms）。

### 20-7. メタバースのアバターでも同じ検証ができるようにした ✅

「メタバースのアバターでもテストをしたい」から。サーボ・LEDの実機と**同じ規則・同じ判定**を、
ブラウザだけで動く Three.js のアバターに載せた（`docs/METAVERSE_AVATAR_TEST.md`）。

- 振る舞いエンジンをJSへ1:1移植（`tools/device/web/wt_core.mjs`）。これで写しが4言語になった
  （Python・簡易参照実装・C++・JS）
- 移植の途中で、`Driver.servo()`/`led()` を**待たずに**呼ぶと、身振りの複数回がブラウザ上で
  同時に発火してしまう問題に気づいた。物理版は1個のマイコンが順に実行するので問題にならないが、
  ブラウザは非同期。3メソッドすべてを `await` 付きの呼び出しに直して解決した
- `npm run device:selftest` に4番目の突き合わせを追加（`node` でJS版を動かし、Python版と
  1手順ずつ・`compare()`の判定まで一致するか確認）。19項目すべて合格
- `drive`（近づく／下がる）は物理版の「前傾で代用」をやめ、アバターでは**実際に位置を動かす**。
  車輪の制約が無い仮想空間ならではの改善として書いた（劣化ではなく利点）
- サンドボックスの回線制限で `cdn.jsdelivr.net` が使えなかったため、Three.jsは
  `tools/device/web/vendor/` に同梱。現場の回線が無くてもこの実験室だけは動く
- `npm run device:avatar` で起動。追加の部品もインストールも不要

---

## フェーズ21: 「依代を持たない人の入口」が、実は2箇所あったのを塞いだ（2026-09-20 実装）

「キーホルダーが無くても、キャラ追加のとき名前入力まで進めてしまう。一般ユーザーも
このUXは良くないのでは」という指摘から。フェーズ19-4で `/add` の3番と `/w` は
既定で閉じたが、**`/t/<code>` と `/q/<code>` そのものには、その閉じ方が一切効いていなかった。**

### 21-1. 何が起きていたか

`/t/<code>` と `/q/<code>` は、台帳（`tag_registry`）に無いコードでも新しい依代として
受け付ける作りになっている（台帳を作る前に配ったタグを死なせないための仕様。
migration 0009_yorishiro.sql）。これは裏を返すと、

```
https://waketama.com/t/好きな文字列
```

を開くだけで、`canCreateCharacter`（src/entryPolicy.ts）や `DIRECT_CREATE_DAILY_LIMIT`
（依代を持たない人の1日3体の上限）を**まるごと素通りして**分身が1体生まれる、
ということでもあった。しかも回数の制限が一切無いので、`/t/1`, `/t/2`, ... と
連番を打てば、1回線からいくらでも作れてしまう。

フェーズ19-4で閉じたのは「誰にでも押せるボタン」で、こちらは「誰でも打てるURL」。
狙っていた場所は塞いだが、隣に塞いでいない入口が残っていた。

### 21-2. 直したこと ✅

`/t/`・`/q/` が**台帳に無い新しいコードを初めて受け付ける瞬間**にだけ、
同じ回線からの回数を数えるようにした（`src/yorishiro.ts` の `canClaimNewTag` /
`TAG_CLAIM_DAILY_LIMIT`）。上限は20/日で、依代を持たない人の上限（3/日）より緩くしてある
——本物の依代を何個も同時に開ける買い方（まとめ買い・イベントでの複数配布）まで
邪魔してはいけないため。**警戒したいのは連番打ちのような機械的な量産だけ。**

- 上限に達したら、DBには何も書き込まずに `/add?closed=1&reason=busy` へ送る
  （コードを無駄に消費させない。busyは「依代をお持ちでない」とは別の理由なので文面も分ける）
- **既に生まれている依代への再タップは数えない。** 新規発行だけが対象
- `npm run test` に検証を追加（`src/__tests__/yorishiro.test.ts`）:
  上限までは `/summon` へ、超えた分は `/add` へ行くこと。既存コードの再タップは
  上限を使い切った後でも通ること

### 21-3. 教訓

「入口を閉じた」と言えるのは、**その入口から分身が生まれる経路をすべて洗い出したとき**
だけ。今回は `/add` の画面（見た目のボタン）だけを見て「閉じた」と判断し、
`/t`・`/q` という別経路が同じ結論（依代を持たない人にも作れる）に到達できることを
見落としていた。次に入口を増やす／閉じるときは、`canCreateCharacter` を通る経路と
通らない経路を先に列挙してから手を付けること。

### 21-4. ENTRY_OPEN を切ることを確認 ✅

キーホルダーの実機がまだ無いあいだ `--var ENTRY_OPEN:1` を付けて `/add` の3番・`/w` を
開けていた（フェーズ19-4）が、プレローンチにあたって**切ってよいと運営に確認済み**。

`ENTRY_OPEN` は `wrangler.toml` の `[vars]` には書かれておらず、デプロイのたびに
`--var` で渡す一時的な値でしかない。**次回、フラグを付けずに `npm run deploy` を実行
すれば、それだけで閉じる**（Cloudflare Workersのvarsはデプロイごとに完全に置き換わり、
前回のCLI引数を引き継がない）。コード側の変更は不要——次のデプロイが実質的な対応になる。

あわせて、`/add` の「1. 依代（よりしろ）をかざす」に商品写真を1枚足した
（`/lp` の商品セクションと同じ `public/media/keyholder.jpg` を仮で流用）。
文字だけの説明だと、依代を持たない人が「自分の持っているものが依代なのか」を
判断できない。撮影前の仮画像であることは `/lp` 側のコメントと同じ注記を添えてある
——現物を撮ったら、同じファイルを上書きするだけで両方のページが差し替わる。

---

## フェーズ22: 法人向けページに料金を載せた（2026-09-22 実装）

「for BizのLPに金額を記載したい。類似サービスを調べて程よい金額で。プランを分けてもよい」から。

### 22-1. 載せたもの ✅

`/biz` に `#pricing` 節を足した（ヘッダー・ヒーローから飛べる）。表示は税別。

| 区分 | 中身 |
|---|---|
| まず試す | サンプル評価 0円（検査用の合成2体の一式） |
| 月額（更新が届く） | ライト 9,800円（1体）／スタンダード 39,800円（5体・全形式再発行）／プロ 98,000円（20体・統計・相談） |
| 1体ずつ（買い切り） | 振る舞い・判断 20,000円／人格カード 30,000円／SLM一式 50,000円／持ち出し一式 80,000円 |
| データ・統計 | セグメント統計 100,000円/回／マスキング済み人格データ 3,000円/体（20体から） |
| 実機検証・共同開発 | 実機検証 300,000円（1か月）／拡張・共同開発 100万円〜／量産搭載 個別見積 |

**金額の定義元は `public/biz.html` だけ。** 決めたときの前提（比較した相手と、なぜこの額か）は
`docs/SALES_DELIVERY.md` §4 に書いた。

- 料金の各ボタンは、問い合わせフォームの「ご関心」を先に選んでおく（`data-topic`）。
  分類を3つ足した（`sample` / `plan` / `persona`）。受け付け側（`src/contact.ts` の `TOPICS`）と
  管理画面の表示名も揃えた。ずれると `other` に丸められて、何の相談か分からなくなる
- 「正直に書いておくこと」の「決済機能は無い」の一文を、料金節への導線に直した
- 「料金」リンクを足したぶん、幅380px以下ではヘッダーの「個人の方へ」を畳む（フッターにある）
- 検証: `persona.test.ts` に新しい分類がそのまま残ることを、E2Eに料金節・税別・ボタンとフォームの対応を追加

### 22-2. 事業判断として残っているもの

> 下の2つ（同意の取り方・回数上限）は、翌日フェーズ23で実装した。

- **他の方が育てた分身を1体単位で売るときの同意の取り方。** 規約の同意は「個人が特定されない形での
  企業向け提供」までで、特定の1体の写しを特定の会社へ渡すことまでは読めない。
  ページには「個別に同意をいただけたものに限る」と書いたが、その同意を取る画面はまだ無い
  （当面は運営から持ち主へ個別に確認する運用）。弁護士確認の項目に足しておくこと
- `persona_reply` の回数上限は**持ち主の会話と共通**（1体あたり1日300回、`src/lib/rateLimit.ts`）。
  月額の買い手が使い切ると、持ち主本人がその日話せなくなる。売り始める前に分けること
- 価格は叩き台ではなく公開値だが、母数が増えたら統計・データの価格は上げる前提（ページにも明記）

---

## フェーズ23: 法人へ渡す写しを、会話と切り離した（2026-09-23 実装）

フェーズ22で残した「1体単位で売るときの同意」に着手したところ、それより手前に穴があった。

### 23-1. 見つけたこと 🐛

**引換券での取り出し・MCP・管理画面の下見が、本人向けの `buildCard` をそのまま使っていた。**
結果として、買い手に次のものが渡る作りになっていた:

- 会話の抜粋（応答例。相手＝持ち主の発言そのもの）と、SLM一式の学習データ（会話の往復そのもの）
- 長期記憶・覚え書き（「柴犬のコタロウを飼っている」「夜勤で働いている」の類）
- 世帯年収、アクセシビリティ設定、分身の識別子
- MCP の `persona_reply` が返す systemPrompt にも、覚え書き・記憶・会話の抜粋が入っていた

規約・プライバシーポリシーの「会話の本文そのものを第三者へ提供しない」「アクセシビリティ設定は提供の対象外」に
正面から反する。`SKUS` の「渡らないもの」にも反していた。テストは振る舞い・判断プロファイルの漏れしか
見ておらず、人格カードは「取り出せること」しか確かめていなかった。販売前に見つかったのは幸運だった。

### 23-2. 直したこと ✅

- **買い手用の入口 `CharacterState.buildBuyerCard()` を分けた。** 会話・記憶・覚え書き・アクセシビリティ設定は
  引数を絞るのではなく、最初から空で組み立てる。年収と、管理画面で後から足した設問も落とす。
  識別子は元へ戻せない値（`p_` ＋ハッシュ）に置き換える。カードに `audience: "buyer"` の印と、README の注記
- 取り出し（`/api/delivery` 全形式）・MCP 3つの道具・管理画面の下見を、すべてこの入口に寄せた。
  本人の書き出し（`/api/persona/card`）だけが `buildCard` を使う
- **同意 `individual`（「この分身の写しを、法人へ渡すことを許可する」）を足した。** 既定オフ・入口の同意に束ねない・
  terms が無い分身では有効にならない・この項目が無かった頃の記録から推定しない。
  これが無い分身は、券の発行（409）・取り出し・下見のすべてで止まる。外すと発行済みの券がその場で失効する。
  `/profile` にトグルと「いま受け取れる法人の件数」を出した
  - **版（CONSENT_VERSION）は上げていない。** 既定オフで流用の余地が無いのに、全員を同意画面へ戻すだけになるため。
    例外の条件は `consent.ts` に書いた（束ねる・既定オンの用途を足すときは、これまでどおり必ず上げる）
- **MCPの回数上限を、持ち主の会話と別の入れ物にした**（`buyerRateLimiter`）。買い手が使い切っても本人は話せる
- 自分で育てた分身向けに、本人の書き出しへ `format=slm`（学習データつき）を足した。
  会話込みの形は本人の経路でしか出ない
- 規約 8-2、プライバシーポリシー 5・6・7・12章、`/biz`（提供物の説明と料金）、`SKUS`、`SALES_DELIVERY.md` を実装に合わせた。
  料金は、買い手向けのSLM一式が学習データ抜きになるので1体買い切りから外し、持ち出し一式を 45,000円 に直した
- テスト（+12）: 全取り出し口と `persona_reply` に会話・覚え書き・年収・識別子が出ないこと、本人の書き出しには残ること、
  同意なしで発行できないこと、取り消すと失効すること、件数が見えること、回数上限が分かれていること、
  同意の正規化（束ねない・推定しない）。**旧実装に戻すと漏れのテストが落ちることを確認済み**

### 23-4. 実ブラウザで確かめて見つけた、CSP（10-8）の穴 🐛 → ✅

画面の確認のため、この環境で初めて実ブラウザ（Chromium）から全ページを2周開いた。10-8 のnonce化は
ブラウザで確かめられていなかったので、次の3つが見つかった。どれもテストは通るのに、本番で黙って壊れる種類。

1. **2回目以降の訪問で、そのページのスクリプトが全部止まる（いちばん重い）。**
   アセット層が ETag を付けて返し、ブラウザの再訪問に 304 を返していた。304 だとブラウザは
   「手元の古い本文（古いnonce）」に「新しいヘッダ（新しいnonce）」を重ねるので、全 `<script>` が拒否される。
   → HTMLを返す経路では条件付きリクエストをアセット層へ渡さず、ETag/Last-Modified を外し、`no-cache` にした
   （管理画面・404 の `no-store` はそのまま）。テストで 304 にならないことと、本文とヘッダのnonceが揃うことを縛った
2. **Service Worker（/sw.js）の登録が拒否されていた。** `worker-src` が無いと `script-src`（nonceだけ）が使われるため。
   → `worker-src 'self'`
3. **視線入力の MediaPipe（`import()` での後読み）が拒否される作りだった。** → `'strict-dynamic'` と、
   WASM のコンパイルだけを許す `'wasm-unsafe-eval'`（JS の eval は許していない。テストで `'unsafe-eval'` が無いことも確認）

直したあと、全17ページ×2周でCSPの拒否が0件、SWの登録も成功を確認した。
（視線入力のCDN読み込みそのものは、この環境の回線制限で届かないため実機で確認すること。CSPでの拒否は出ていない）

あわせて `/profile` の「はじめる前に」の注記に `**` が記号のまま出ていたのを直した。

### 23-3. 残っていること

- 規約・ポリシーの文面を変えたので、**弁護士確認の対象に今回の差分を含めること**
- すでに発行済みの引換券があれば、この変更で取り出せなくなる（持ち主が `individual` を入れていないため）。
  プレローンチ前なので実害は無い想定だが、管理画面の一覧で有効な券が残っていないか一度見ること
- マスキング済み人格データ（まとめて提供）は手作業の運用で、`buildBuyerCard` を通す仕組みがまだ無い。
  出すときは、この入口で作ったものだけを束ねること

---

## フェーズ24: キャラクターを10種族（60体）増やし、声を分けた（2026-09-23 実装）

「キャラクターも10個ほど追加して。色違いで6色ずつ、合成音声の違いを出すのも忘れずに」から。

### 24-1. 足した種族 ✅

ほしぽ・きのこん・たまとり・みみぴょん・ふたばる・くらげっこ・にゃまる・かめんこ・ぽんぽこ・ふたたま。
各6色で、画像（PNG）と3Dモデル（GLB）を60組。**これで15種族×6色＝90通り。** 通常の誕生に混ざる（等確率）。

- 生成は `tools/characters/make_characters.py`（`--sheet` で一覧画像）。既存の30体から色・縁取り・顔つきを実測して揃えた。
  **既存の5種族は作り直さない**（既存の分身の見た目が変わるため。スクリプトも触らない作り）
- 3Dは既存と同じ「骨なし・頂点色」の形・同じ大きさ（幅1.3前後、顔は +Z）。chat の model-viewer とメタバースでそのまま出る
- 種族の並びと名前は、サーバー（`SPECIES_KEYS`/`SPECIES_LABELS`）と画面（新設の `public/species.js`）の2か所だけ。
  図鑑（home）・出会いの図鑑（friends）・限定の姿の設定（admin）が各自で書き写していたのを、`species.js` を読む形に直した。
  **両者の一致と、90通りすべての PNG/GLB の存在をテストで確かめる**（足し忘れると姿が出ない）

### 24-2. 声 ✅

- 10種族それぞれに声の家系（高さ・速さ・端末の声の選び方）を足した（`src/ai/voiceProfile.ts`）。
  **90通りすべてが違う声になる**ことと、どの種族も既定の声に落ちていないことをテストで確かめる
  （最初の案では3組がぶつかっていた。速さをずらして解消）
- 高音質（MeloTTS）は声を1種類しか返さないので、どの子も同じ声になっていた。
  **再生側で速さと音程を分身ごとに変える**（`voiceSpeak.js` の `voicePlaybackRate`、`preservesPitch = false`、0.9〜1.2倍）。
  chat・talk・call とメタバースで効く

---

## フェーズ25: 複数人のメタバースを作った（2026-09-23 実装）

「モバイルでも動くメタバース。何を映すかを設定可能に。わけたまの人格データが動く環境」
「設定は既存の管理画面に、メタバースはキャラクター画面から遷移。広告やミニゲームなど幅広く」から。
詳細は `docs/METAVERSE.md`。

### 25-1. 作ったもの ✅

- **入口**: キャラクター画面（/chat）の「🌐 メタバース」→ `/meta?cid=…`。その子と（この端末の子を合わせて3体まで）部屋へ
- **部屋の設定は管理画面「メタバース」タブ**だけで作る（利用者は作れない）。場所5種×時間帯4種・最初の視点4種・
  置く物最大7つ（看板＝広告・お知らせ／動画／いまいる子の紹介／宝さがし／○×クイズ／スタンプラリー）。
  保存すると、部屋にいる人の画面もその場で変わる
- **広告の看板には「広告」の札が自動で付く**（外せない）。リンクは確認のうえ別タブ・`noopener,noreferrer`
- **ミニゲーム3種**（`public/meta/games.mjs`）: どれも分身を歩かせて遊ぶ。クリアするとその子が自分の声で喜び、
  同じ部屋の人には拍手のスタンプが届く。結果はサーバーに残さない（自己ベストだけ端末に）
- **複数人**: Durable Object `MetaverseRoom`（WebSocket Hibernation。1部屋12端末まで）。入室は持ち主トークンで本人確認し、
  他の人に配るのは名前・姿・声・動きの数値（compact）・位置だけ。移動・スタンプ・挨拶を中継。
  **利用者どうしの自由な文字は通さない**（挨拶は決まった台詞の番号、スタンプは決まった種類）。入退室の記録は持たない
- **人格どおりに動く**: 配った数値を、実機の検証機と同じエンジン（`wt_core.mjs`）に通す。近づく・待つ・下がる、
  返すまでの間、手を振る回数が、その子の性格と価値観の方針で分かれる
- **スマホで動かす工夫**: three.js を同梱（CDNに頼らない）、GLTFLoader を使わず頂点色だけ読む小さな読み込み器、
  影・後処理なし・光2つ、解像度は最大1.75倍、画面を隠すと描画停止。縦長の画面では少し引いて左右の屋台が入るように
- 最初からある部屋: わけたまのひろば（看板・宝さがし・○×クイズ・紹介・スタンプラリーの見本）、夜空の丘

### 25-2. 確かめたこと ✅

- テスト（+11）: 他人に配る情報に識別子・トークン・覚え書きが入らない、持ち主でなければ連れて入れない、
  他人の子は動かせない、自由な文字が中継されない、同じ子を重ねて入れない、よその Origin を断る、
  置く物の検査（http のURL・置き場所の重なり・問題の無いクイズ・数の範囲）、利用者が部屋を作る口が無い
- 実ブラウザ（スマホ幅・2人同時）: 互いの子が見える／動きが届く、屋台の輪に入ると案内→宝さがしクリア、
  ○×クイズの判定、管理画面で部屋を作る→入る→管理画面で直すと部屋の中が変わる、15種族すべての3Dモデルが表示される

### 25-3. 残っていること

- **スマホ実機での確認**（この環境はソフトウェア描画で、フレームレートは参考にならない）。2台で同じ部屋に入ること
- 規約（10-2）・プライバシーポリシー（11-2）に足した文面を、弁護士確認に含めること
- ミニゲームの結果を景品に結びつける企画は、結果をサーバーに持たない前提で組む（画面を見せてもらう運用）

---

## フェーズ26: メタバースに入れない不具合を直し、センサー・XRのミニゲーム10種とエリア管理を足した（2026-09-23 実装）

「この端末の分身だと確かめられませんでした、となってメタバースに入れない」
「メタバース・XRを活用したミニゲームをあと10個。ジャイロやモバイルのセンサーを使って」
「管理画面でミニゲーム管理とメタバース管理を。二つを一つにしたり、新しいエリアの開放をしたりしたい」から。

### 26-1. 入れない不具合 ✅

- **原因の切り分けができなかった**: 部屋は「持ち主でない」「同意が済んでいない」「記録に失敗した」を、
  すべて同じ「この端末の分身だと確かめられませんでした」で返していた。手元の実ブラウザでは正常に入れたため、
  利用者の端末で起きうる原因（同意の版が上がった後まだ開いていない子／持ち主の印が無い・古い／apex の `waketama.com/meta` で開いて
  持ち主の印が見えない）を全部塞いだ
- 断った理由を種類で返す（`not_owner` / `no_token` / `no_consent` / `not_found` / `already_here` / `unavailable` / `audit_failed`）。
  画面は理由ごとに直し方を出し、同意・持ち主の問題なら「キャラクター画面を開く」へ案内。一部の子だけ入れないときは、入れた子で入って知らせる
- 入る前に「はじめる前の同意」を確かめる（`gate.js`。入ってから断られない）
- `/meta` をアプリのホスト専用にした（`waketama.com/meta` → `app.waketama.com/meta`）

### 26-2. センサー・XRのミニゲーム10種 ✅（`public/meta/sensorgames.mjs` / `sensors.mjs`）

かたむけコロコロ（ジャイロ）・ふりふりダッシュ（加速度）・ゆらゆらバランス（ジャイロ）・こえでジャンプ（マイク）・
カメラでARさがし（カメラ＋ジャイロ）・ぐるっと星キャッチ（ジャイロ）・ふりふりリズム（加速度）・
ホット＆コールド（向き＋振動）・だるまさんがころんだ（加速度）・ARでおでかけ（WebXR）。

- どれも自分の分身（同じ種族・色の3Dモデル）が主役。クリアすると部屋に戻って、その子が自分の声で喜ぶ
- iPhone の「動きと向き」の許可は「はじめる」のタップの中で求める。**センサーが使えない・断られたときは画面の操作で遊べる**
- 本物のARは WebXR `immersive-ar`＋`hit-test`（床を見つけて分身を出す）。非対応端末はカメラの映像の上で遊ぶ
- **センサーの値・声・映像は端末の中だけで使う**（送らない・記録しない。マイクは大きさだけ、カメラは背景に映すだけ）。
  プライバシーポリシー 11-2 に追記。`Permissions-Policy` に傾き・動き・AR を自分のページだけ許可
- 目標・制限時間・むずかしさ（1〜3）・クリアの言葉は、置くエリアごとに管理画面で決める（範囲は `SENSOR_GAMES`）

### 26-3. メタバース管理・ミニゲーム管理 ✅

- **エリアの状態**: 公開／準備中（管理者だけ）／閉鎖＋開放日時（前は「近日開放」）・終了日時（過ぎたら閉鎖）・並び順（`0013_metaverse_areas.sql`）
- **エリアをまとめる**: まとめ先＋まとめる元を選ぶと、置く物を合わせた下書きが開く。まとめた元は `merged_into`。
  古いリンクで来た人・いま入っている人はまとめ先へ案内される。まとめ先を消すと、まとめは自動で解ける
- **最初からあるエリアも直せる**（同じIDの行で上書き。「元に戻す」で最初の状態）
- **ミニゲーム管理タブ**: 出す／止める（止めると置いたままでもどのエリアにも出ない。開いているエリアからもすぐ消える）、
  既定値、使っているエリア、「試す」（そのゲームだけの試し場。準備中＝管理者だけ）

### 26-4. 確かめたこと ✅

- テスト（+11）: 断った理由の種類（not_owner / no_consent）、一部だけ入れないときの welcome.notices、
  開放日時の前は一覧に「近日開放」で入れない、終了・閉鎖・準備中は入れない、まとめた元のリンクはまとめ先へ・入室は movedTo つきで断る、
  まとめ先を消すとまとめが解ける、最初からあるエリアの上書きと元に戻す、10種の範囲の検査、止めたミニゲームは出ない、
  管理のAPIは管理者だけ、いる間にまとめられたら moved が届く、`waketama.com/meta` はアプリのホストへ
- 実ブラウザ（スマホ幅・センサーは合成したイベント・カメラとマイクは擬似デバイス）: 10種すべてが始まり・動き・「やめる」で
  部屋に戻り、カメラ・マイクが止まること。管理画面で開放予約→「近日開放」、ひろばと夜空の丘をまとめる→夜空の丘のリンクがひろばへ、
  ミニゲームを止める→エリアから消える、元に戻す
- E2E: センサーのゲームが定義と画面の両方に10種そろっている、ロビーに状態が付く、`Permissions-Policy`

### 26-5. 残っていること

- **スマホ実機での確認**（iPhone の許可ダイアログ、Android の WebXR AR、加速度のしきい値＝ふる・足ぶみの感度）。
  しきい値は `sensors.mjs` の `Motion`（ふる 13m/s²・足ぶみ 2.6m/s²）と各ゲームの `level`
- プライバシーポリシー 11-2 のセンサーの追記を、弁護士確認に含めること
- 本番へ出す前に `npm run db:migrate:remote`（0013）

---

## フェーズ5: 公開に向けた運用・法務

一般公開するなら機能より先に必要になる項目です。

- ~~**プライバシーポリシー**~~: 作成済み（`public/privacy.html` → `/privacy`）。会話・音声・カメラ映像・
  「これ見て」で送る静止画の扱いまで書いてあります。**空欄も埋めた**（事業者名: Straid、
  連絡先: shotake0222@gmail.com）。**残るは弁護士等の確認だけ**
- ~~**利用規約**~~: 作成済み（`public/terms.html` → `/terms`）。禁止事項・免責・分身データの扱い・
  マーケット・サービス終了時の扱いまで含めてあります。**空欄も埋めた**（事業者名: Straid、
  連絡先: shotake0222@gmail.com、管轄裁判所: 東京地方裁判所）。**残るは弁護士等の確認だけ**
- ~~**年齢への配慮**~~ 見直し済み（2026-09-21）。やったことと、あえてやらなかったことの両方を書く:
  - **入口の同意画面に注記を追加**: `利用規約`12条・`プライバシーポリシー`16条には
    「未成年の方は保護者の方の同意を得たうえでご利用ください」が既にあったが、
    そこは同意画面からはリンクの先——実際にはほとんどの人（特に子ども）が開かない。
    NFCキーホルダーという入口の性質上、子どもが直接この画面に来る可能性があるため、
    `src/persona/consent.ts` の同意画面本文（`gate.js`が表示するもの）自体に同じ文言を足した。
    文面を変えたので、このファイルの方針どおり`CONSENT_VERSION`を3→4に上げ、既存の同意は取り直しにした
  - **収集情報の再確認**: `src/persona/profile.ts`の設計方針（自由記述を置かない・
    要配慮個人情報を取らない）を読み直したが、これは元から「取るべきでない情報」を
    ブロックする設計になっており、追加の変更は不要と判断した
  - **「苦手なこと」の選択肢にある「下ネタ」「強い言葉」はあえて残した**: これは分身に
    「話さないでほしい話題」を教えるための、本人保護のための選択肢（地雷を踏ませないため）であり、
    かつ15回会話したあとにしか出ない・答えなくてよい任意項目。取り除くより、
    子どもが選べば分身がその話題を避けるようになる、という現状のほうが安全側だと判断した。
    ここは価値判断が入るので、違う結論であれば`src/persona/profile.ts`の`petPeeve`を直してください
  - **onboarding画面（`/add`・`/summon`等）の文面**は、既存の「わけたま」らしい平易な言葉遣いを
    確認したが、書き直しが要るような表現は見当たらなかった。もし気になる文言が具体的にあれば教えてください
- ~~**不適切な入力への対応**~~ 実装済み（2026-09-21）。「モデル任せ」をやめ、2段構えの方針にした:
  1. **自傷・自殺のサインだけは、AIの生成に任せない**（`src/ai/safetyGuard.ts`）。
     単純な正規表現での検知だが、該当したら`/api/chat`・`/talk`・`/api/call/stream`のどの経路でも
     AIを呼ばずに固定文面を返す。窓口は「よりそいホットライン」（0120-279-338、24時間・無料）と
     チャット相談の「あなたのいばしょ」（厚労省の案内、2026年9月時点で確認）を案内する。
     検知した事実だけを`countMetric`で集計し（内容はログに出さない）、
     半年に一度は窓口情報が古くなっていないか見直すようコメントに残した
  2. **それ以外の不適切な要求**（性的な内容・暴力や違法行為の助長・相手を傷つける言葉）は、
     機械的に検知すると誤検知が多すぎるため、`src/ai/promptBuilder.ts`の`SAFETY_RULES`として
     システムプロンプトに持たせた。人格を保ったまま、その子らしい言葉で短く断る方針
  - **方針を`public/terms.html`に明文化**した（新設した5-2節）。AIが応答を生成しないことがある旨と、
    分身は相談窓口の代わりにはならない旨を明記した
  - テストは`src/__tests__/safetyGuard.test.ts`（検知そのもの）と、`index.test.ts`・`call.test.ts`
    （実際にAIが呼ばれないことを、AI呼び出しをモックして確認）に追加した
  - **やらなかったこと**: 自傷・自殺以外のカテゴリ（性的内容や暴力など）を機械的に検知して
    ログに残す仕組みは作らなかった。誤検知（普通の会話やフィクションの話題を弾いてしまう）の
    リスクの方が大きいと判断したため。運用しながら実例が見えてきたら、個別に足す方が安全
- ~~**問い合わせ窓口**~~: 実施済み。法人向け（`/biz`）と復旧（`/recover`）の窓口を用意し、
  届いたものは管理画面で対応状況まで管理できる

---

## 残っている作業（優先度順）

1. ~~**利用規約とプライバシーポリシーの空欄埋め**~~ 完了（2026-09-20）: 事業者名（Straid）・
   連絡先（shotake0222@gmail.com）・管轄裁判所（東京地方裁判所）を埋め、両ページ冒頭の
   「下書きです」の節も消した。`npm run legal:check` は空欄0件。
   **残るタスクは弁護士等の確認を受けること**（公開前に必須。当方では代われない）
2. **実機での音声確認**: 音声入力はコードとしては入っているが、iOS Safari / Android Chromeでの
   挙動は実機でしか分からない。マイク権限、無音時の扱い、ハンズフリーの回り方を確認する
3. ~~**読み上げの画面統合**~~ 実施済み（2026-09-21、1-2）: 4画面すべてに「声質：標準／高音質」の
   切り替えとして追加した。**残るタスクは実際に聞いてから、既定をどちらにするか決めること**
   （日本語の品質と遅延は、文章だけでは判断できない）
4. ~~**CSPの本採用**~~ 実施済み（2026-09-21、10-8）: nonce方式に変え、`script-src`を追加した。
   **残るタスクは、実ブラウザ（実機かリモート接続できるwrangler dev）でCSP違反が出ないか一度確認すること**
5. **キーホルダーの試作と、実機での読み取り確認**（現物が要る）
6. ~~Service Workerの緊急停止手段（0-6）~~ 実装済み（10-4）
7. ~~iOS向けのホーム画面追加の案内~~ 実装済み（16-2）
8. ~~性格更新ロジックのチューニング~~ 実施済み（16-3）。実ログを見ながらの再調整は今後も続く

## 進める順番の提案（当初計画）

```
フェーズ0（0-1〜0-3）  ← ドメイン反映後すぐ。ここが無いと以降のデバッグが苦しい
   ↓
フェーズ2（2-1・2-2）  ← 「残さない」土台。テストを先に書く
   ↓
フェーズ2（2-3）＋フェーズ1（1-1）  ← ストリーミングと音声入力
   ↓
フェーズ2（2-4）      ← 通話UIとして統合。ここで「分身と電話する」が完成
   ↓
フェーズ0（0-7）＋フェーズ5  ← 公開前に必須
   ↓
フェーズ3・4          ← 継続的な改善
```

音声とその場限りモードは最終的にひとつの体験（分身との通話）に合流するので、
別々に作り込むより、上記の順で土台から積むほうが手戻りが少なくなります。
