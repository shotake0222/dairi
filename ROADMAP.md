# わけたま 開発ロードマップ

最終更新: 2026-09-13（フェーズ0〜3、6「かざして話す」・会話品質、7「人格データ基盤」を実装済み）

本番ドメイン（waketama.com）が動き出すと、これまでのように手元だけで完結しなくなります。
実機で起きた不具合を追える状態を先に作ってから機能を足す、という順序でまとめました。

---

## 現在地

**動いているもの**: NFCタップ→召喚→会話、性格の育成と成長グラフ、長期記憶（Vectorize）、
分身同士のお散歩（オプトイン制・自動実行）、出会いの図鑑、分身のホーム画面、人格のエクスポート/インポート、
持ち主トークンによる保護、分身の削除、PWA、OGP。テストは84件（vitest）＋実機E2E。

**2026-09-12に実装したもの**: 構造化ログと `/api/health`、版数の刻印、保存に依存しないレート制限、
その場限りの通話モード（SSEストリーミング・通話UI）、音声入力（Web Speech API + Whisper）、
読み上げAPI、引き継ぎコード、ステージング環境。テストは114件（vitest）＋実機E2E。

**まだ無いもの**: 利用規約・プライバシーポリシー（音声を扱う以上、公開前に必須）、
iOS向けのホーム画面追加の案内、性格更新ロジックのチューニング、Service Workerの緊急停止手段。

---

## フェーズ0: 本番を「デバッグできる」状態にする（最優先）

機能追加より先にここを固めないと、実機で起きた不具合が「再現できません」で終わります。
ドメイン反映後、最初のデプロイと同時に着手する想定です。

### 0-1. Workers Logs を有効化 ✅ 設定済み（要デプロイ）

`wrangler.toml` に `[observability]` を追加済みです。次回のデプロイで有効になり、
Cloudflareダッシュボードから過去のリクエストとログを検索できるようになります。

リアルタイムで追う場合は以下。実機を触りながら別画面で流しておくのが基本形です。

```bash
npx wrangler tail --format pretty
npx wrangler tail --status error          # エラーだけ
npx wrangler tail --search "characterId"  # 文字列で絞る
```

### 0-2. リクエストIDと構造化ログ ✅ 実装済み

現状のコードは失敗を握りつぶす設計（AIやVectorizeが落ちても会話は続く）にしてあります。
これは体験としては正しいのですが、**失敗しても何も残らない**ため、
「なんとなく記憶が弱い気がする」といった不具合の原因追跡ができません。

- 握りつぶしている `catch` に `console.warn` を入れ、どこで何が落ちたかを残す
- 1リクエストにIDを振り、DO・AI・Vectorizeのログを串刺しで追えるようにする
- 個人情報（会話本文）はログに出さない。出すのは種別・所要時間・成否のみ

### 0-3. `/api/health` と バージョン表示 ✅ 実装済み

D1・Vectorize・AIそれぞれに疎通確認をかけ、結果をJSONで返すエンドポイント。
「Vectorizeのインデックスを作り忘れていて記憶が保存されていなかった」類の事故は、
これがあると1秒で分かります。

あわせて、デプロイのバージョン（コミットハッシュ等）をレスポンスヘッダと `/api/health` に載せます。
**PWA化した以上これは必須です**。端末に古いService Workerやキャッシュが残っている状態で
「直したはずのバグが直っていない」と悩むのを防げます。

### 0-4. ステージング環境 ✅ 実装済み（初回のD1作成のみ手作業）

本番のD1に混ざると、テストで作った分身が「お散歩」の相手候補として実ユーザーに出てしまいます。
`wrangler.toml` に `[env.staging]` を追加し、別Worker・別D1・別Vectorizeインデックスに分けます。

```bash
npx wrangler deploy --env staging   # staging.waketama.com などに出す
```

### 0-5. 実機デバッグの手順書（見積: 1時間）

NFC・カメラ・マイク・PWAインストールは実機でしか確認できません。手順を残しておきます。

- iPhone: Mac の Safari →「開発」メニューから実機のSafariに接続してコンソールを見る
- Android: Chromeで `chrome://inspect` から実機を開く
- NFCタグの書き込み: iPhoneは「NFC TagWriter」、Androidは「NFC Tools」

### 0-6. Service Worker の緊急停止手段（見積: 1時間）

PWAで最も怖い事故は、壊れたService Workerが端末に居座って更新を配信できなくなることです。
現状のSWはHTMLとAPIをキャッシュしない設計なので危険度は低いのですが、保険として
「登録解除だけを行うSW」を配信できる状態（kill switch）を用意し、手順を残します。

### 0-7. コスト上限とレート制限 ✅ 実装済み

Workers AIは呼ぶたびに課金されます。現状の防御は「連投ガード（1.2秒）」と
「お散歩のクールダウン（20時間）」だけで、**1人が延々と話し続けるコストは止められません**。

- 1分/1日あたりの上限を設ける（Durable Object内でカウント）
- Cloudflare側の課金アラートを設定する
- 上限に当たったときの文言を、キャラクターの世界観を壊さない形で用意する（「少し眠くなってきた…」等）

---

## フェーズ1: 音声でのやり取り

現状は**読み上げ（出力）だけ**が実装済み（Web Speech APIの音声合成、性格で速さと高さが変わる）。
足りないのは音声入力と、会話として成立させるつなぎ込みです。

### 1-1. 音声入力 ✅ 実装済み（実機での確認は要）

二段構えを推奨します。

1. **Web Speech API（`SpeechRecognition`）**: 無料・低遅延。Chrome系で良好。まずこれを使う
2. **Workers AI Whisper へのフォールバック**: `MediaRecorder` で録音してWorkerへPOSTし、
   サーバー側で文字起こし。iOS Safariなど1が不安定な環境向け

**デバッグ観点**（ここは実機でしか分かりません）:
- マイク権限の拒否・取り消し時に、無言で壊れず案内が出るか
- iOS Safariは仕様変更が多い。実機での確認を前提に、失敗したらテキスト入力へ静かに戻す
- 無音・雑音のみのときにAIを呼ばない（コストと誤爆の防止）
- 方言や固有名詞の誤認識をどう扱うか（確定前に編集できるUIにするか）

### 1-2. 読み上げ品質の向上 △ APIのみ実装（画面からはまだ使っていない）

ブラウザ標準の音声合成は端末によって声が大きく変わり、機械的です。
Workers AI の音声合成モデル（日本語対応のもの）に差し替えると、分身ごとに声を割り当てられます。

- 生成した音声のキャッシュ（同じ台詞を何度も生成しない）
- コストと遅延が増えるため、標準音声との切り替え設定を残す
- **デバッグ観点**: iOSは「ユーザー操作なしの音声再生」を禁止しています。
  自動読み上げが無音で失敗する典型パターンなので、最初の再生は必ずタップ起点にする

### 1-3. ハンズフリー会話ループ ✅ 実装済み

「話す→文字起こし→返答→読み上げ→また話す」を自動で回すモード。
フェーズ2のストリーミングと組み合わせると、後述の「電話」体験になります。

---

## フェーズ2: その場限りのチャットモード（Jiffcy的な「テキスト通話」）

参考にされた **Jiffcy（ジフシー）** の要点は、①電話のように“繋いで”話す、
②入力中の文字が1文字ずつ相手に見える、③既読が無い、④履歴が残らない、の4点です。
これを「相手＝自分の分身」に置き換えると、**「分身と電話する」体験**になります。

### 2-1. サーバー側: 何も残さない会話 ✅ 実装済み

`POST /api/chat` に `ephemeral: true` を足すのではなく、
**専用の経路を用意するほうが安全**です。理由は、既存のchat()は性格更新・履歴追加・
記憶保存・ディレクトリ同期まで一気に行うため、条件分岐で「書かない」を足すと
将来の変更で簡単に漏れるからです。

設計方針:

- Durable Object は **読み取り専用**（`getState()` のみ）で使う
- AI呼び出しはWorker側で行い、`storage.put` も `storeMemory` も一切通らない経路にする
- 記録しないもの: 会話本文、記憶（Vectorize）、性格の変化、やり取り回数、成長段階、最終訪問日時
- 参照はする: 現在の性格と口調（そうしないと「自分の分身」ではなくなるため）。
  「何も思い出さないモード」は後から選択制で足せる
- **レート制限の作り直しが必要**: 現在の連投ガードは `lastMessageAt` の書き込みに依存しています。
  何も書かないモードでは効かないので、DOインスタンスのメモリ上でカウントする方式に変えます

### 2-2. 「本当に残っていない」ことをテストで担保する ✅ 実装済み

この機能は「残らないと言ったのに残っていた」が最悪の事故です。信用を直接壊します。
実装より先に、この検証を書く価値があります。

- その場限りモードで会話 → DOのストレージが**1バイトも変化していない**ことを確認
- `env.AI.run` / `MEMORY_INDEX.upsert` のスパイで、記憶保存が**呼ばれていない**ことを確認
- 会話後にエクスポートしたパッケージに、その会話が含まれていないことを確認
- 会話後の「今日の分身レポート」「出会いの図鑑」に痕跡が出ないことを確認

### 2-3. 応答のストリーミング ✅ 実装済み（その場限りモードのみ。通常チャットは未対応）

Jiffcyの「1文字ずつ」に相当する部分。今は返答が出来上がってから一括で表示しているので、
待ち時間が沈黙になります。Workers AI のストリーミング応答をSSEで流し、
分身の言葉が少しずつ現れるようにします。

- その場限りモードはDOに書き込まないため、Worker側で完結でき、実装が素直です
  （通常モードは保存処理があるため、後追いで対応）
- Service Workerは `/api/` を素通しする設計になっているので、SSEを壊しません（対応済み）
- **デバッグ観点**: 途中で回線が切れたとき、書きかけの吹き出しが残らないようにする

### 2-4. 「通話」UI ✅ 実装済み

- 通話画面らしい見た目（背景を落とし、分身を中央に、経過時間を表示）
- 入力中の文字が分身側にも見えている演出（分身が「聞いている」感じを出す）
- 退出時に画面上のログを完全に消去し、戻るボタンでも復元されないようにする
- **「この会話は保存されません」を、開始時と終了時にはっきり伝える**（信頼の要）
- 音声（フェーズ1）と組み合わせると、そのまま音声通話になります

---

## フェーズ3: 所有権の引き継ぎ（現状の弱点）

いまの持ち主トークンはlocalStorageにしか無いため、**機種変更やブラウザのデータ削除で
自分の分身の所有権を永久に失います**。育てるサービスとしてこれは痛い。

### 3-1. 引き継ぎコード ✅ 実装済み

アカウント登録を入れずに解決する現実的な案です。

- 持ち主の端末で「引き継ぎコード」を発行（有効期限つきの短い文字列）
- 新しい端末でそのコードを入力すると、持ち主トークンが移る
- 発行時に「このコードを他人に渡すと分身を乗っ取られます」と明示する

将来的な本格的アカウント（Cloudflare Access等）は、収益化やBtoB提供の段階で検討。

---

## フェーズ4: 体験の質を上げる

- **性格更新ロジックのチューニング**: 係数はまだ仮の値。実ログを見ながら調整（要: フェーズ0の観測性）
- ~~**signalExtractor の高度化**~~: 実施済み。話題の新規性を「疑問符が付いているか」ではなく
  「これまで出てこなかった語か」で測るようにした（分身が既に持っている語彙と突き合わせる）。
  絵文字の感情、強調の段階、笑い表現の判定も見直した。AI呼び出しは増やしていない
- ~~**会話モデルの見直し**~~: 実施済み。ただし「モデルが小さいこと」は主因ではなく、
  **直近の会話をAIに渡していなかったこと**が「会話が頭悪い」の正体でした（詳細は下のフェーズ6）。
  モデル選定は `src/ai/modelPolicy.ts` に集約し、`--var CHAT_MODEL:<ID>` で差し替えられるようにしてあります。
  残っているのは**実会話を見ながらの評価**です（どのモデルが日本語のキャラクター演技に向くかは、
  ベンチマークより実際の会話ログで判断すべきなので、しばらく使ってから決めるのが妥当です）
- **iOS向けの「ホーム画面に追加」案内**: iOS Safariは `beforeinstallprompt` 非対応のため、
  現在Androidにしか案内が出ていない。共有メニューからの手順を出す
- ~~**出会いの図鑑のページング**~~: 実施済み。12件ずつ読み込む
- ~~**記憶削除の取りこぼし**~~: 実施済み。1回のクエリで済ませていたため200件を超える分が残っていた。
  無くなるまで繰り返すようにした（上限5000件。超えたらログに残る）

---

## フェーズ6: かざして話す／会話品質（2026-09-13 実装）

### 6-1. 会話が噛み合わない原因の特定と修正（済）

「会話が頭悪い」の原因はモデルの大きさではありませんでした。`chat()` がAIに渡していたのは
**システムプロンプトと、今回の1発言だけ**で、会話履歴を一切渡していませんでした。
文脈の代わりに使っていた `memorySummary` は、各発言を40文字に切り詰めた1行要約の羅列で、
モデルから見ると毎ターンが初対面でした。直したのは次の3点です。

- **直近のやり取りを原文で保持し、`messages` に user / assistant として積む**（`recentTurns`）
- **「覚え書き」の蓄積**（`src/ai/reflection.ts`）: 数ターンごとに、相手について長く覚えておくべき事実だけを
  AIに蒸留させて溜め、常にプロンプトに載せる。会話を重ねるほど厚くなるので、**育てるほど話が通じるようになる**
- **成長段階で文脈量を変える**（`contextBudgetFor()`）: 生まれたては短く素朴に、育つほど思い出す量と
  話せる長さが増える。モデルを弱くして「バカな時期」を作るのではなく、渡す記憶の量で差をつけている

あわせて、モデルの選定・フォールバック連鎖・環境変数での差し替えを `src/ai/modelPolicy.ts` に集約しました。

### 6-2. かざして話す（済）

カメラのライブビューを表示したまま、同じ画面でマイクを開き、撮像画像を表示したまま応答を
重畳表示＋読み上げする多ターンのモード（`/talk`）。サーバーは文章だけでなく**演出スクリプト**を返し、
ブラウザがそれを実行します。分身ごとの固有の声（characterIdから一意に決まる）も実装しました。
「👁 これ見て」を押したときだけ、その瞬間の1枚を視覚モデルに渡して会話の文脈に混ぜます（画像は保存しない）。

特許ファミリーの構成要件との関係は `特開2024-129022_クレーム対応メモ.md` の6節を参照してください。

### 6-3. 残っていること

- **実機確認**: カメラとマイクを同時に使うため、iOS Safari / Android Chrome での挙動は実機でしか分かりません。
  特に「読み上げ中にマイクが自分の声を拾ってしまう」問題（エコー）は、実機で確認が必要です
- **会話品質の再評価**: 上記の修正後に、実際の会話でどれだけ良くなったかを見てからモデルを決める
- **覚え書きの見え方**: いまユーザーからは見えません。「分身が自分について何を覚えているか」を
  見られて、間違いを直せるようにするのは、体験としても信頼の面でも価値があります

---

## フェーズ7: 人格データ基盤・アクセシビリティ・事業面（2026-09-13 実装）

### 7-1. 同意を土台に置いた（済）

属性の取得・統計利用・出品を、**用途ごとに分けた同意**の下に置いた（`src/persona/consent.ts`）。
既定はすべてオフ。文面を変えたら版を上げて取り直す。集計用レジストリ（D1）には
**同意した分身しか行が存在せず**、同意を外すと行ごと消える。

### 7-2. サイコグラフィックとセグメンテーション（済）

- 語の出現から関心を更新する無料の層（`analysis/textMining.ts`）と、
  数ターンごとにAIで価値観を推定する層（`analysis/psychographics.ts`）の二段構え
- どちらも指数移動平均で少しずつ動かす（1回の会話で人物像が入れ替わらないように）
- 性格＋価値観＋関心から7つのセグメントへ分類（`analysis/segments.ts`）。
  会話量が少ないうちは確からしさが低く出るようにしてある

### 7-3. 人格のDB化とエッジAIへの持ち出し（済）

`src/persona/personaCard.ts`。JSON / システムプロンプト / Ollama Modelfile / 説明書の4形式で出せる。
載せ先のモデルに依存しないよう、記憶は平文、口調は実会話から抜いた応答例で移植する。

### 7-4. 属性・管理画面・マーケット・LP・視線入力（済）

- 属性は選択肢式・全て任意・段階的（`persona/profile.ts`、`/profile`）
- 管理画面は合言葉で保護し、未設定なら開かない（`/admin`）。会話の本文は管理者からも見えない
- マーケットは本人出品と匿名集約セグメントの両方（`/market`）。統計には人数の下限を強制
- toC向けLP（`/lp`）。人格データの売買には触れない方針
- 視線・スイッチによる文字入力（`/eyes`）。8面を3階層たどって1文字、変換はAIに任せる

### 7-5. 残っていること

- **決済**: マーケットは購入希望の連絡を受け取るところまで。金銭のやり取りは未接続。
  値付けより先に「誰に渡してよいか」の運用を決める必要がある
- **視線入力の実機確認**: ウェブカメラの視線推定は個人差が大きい。較正の回数・滞留時間の初期値は、
  実際に使う人に合わせて調整が要る。スイッチ操作へ落ちる経路は用意してある
- **法人向けの統計提供の運用**: 画面はあるが、提供の単位・期間・契約は未定
- ~~**利用規約**~~: 作成済み（`/terms`）。空欄を埋めたうえで弁護士の確認を受けてください

---

## フェーズ8: 訴求と運用の整備（2026-09-13 実装）

- **利用規約**（`/terms`）: 持ち主トークンによる所有権の前提、AI生成物の扱い、マーケット、
  サービス終了時にデータを書き出せる期間を設けること、まで含めた
- **toC LP の作り直し**（`/lp`）: 「分け御霊」という名前の由来を軸に据え、
  文字・声・通話・かざして話す・視線という5つの話しかけ方で分身を形づくっていく、という構成に変更
- **法人向け LP**（`/biz`）: 大規模LLMを常時呼べない現場に向けて、人格を重みではなくデータで持つ構成を訴求。
  問い合わせフォームから直接相談を受けられる（`contact_requests`）
- **覚え書きの書き直し**（`/profile`）: AIが書き留めた内容に間違いがあれば本人が直せる。
  直した直後に蒸留で上書きされないよう、反映済みとして扱う
- **iOS向けのホーム画面追加の案内**: `beforeinstallprompt` 非対応のため、
  iPhone・iPad の Safari でのみ、共有メニューからの手順を表示する

---

## フェーズ9: 復旧と積み残しの解消（2026-09-13 実装）

### 9-1. 分身の復旧（済）

機種変更に失敗して持ち主トークンを失った人を、運営が本人確認のうえで救済できるようにした。

- 利用者側の窓口: `/recover`。届いたものは問い合わせと同じ受け皿に入る（見る場所を増やさない）
- 運営側: 管理画面の「分身の復旧」タブ。タグIDか分身IDで引き、本人確認用の項目を見て、
  理由を書いたうえで引き継ぎコードを発行する
- **復旧に会話の閲覧は要らない**。所有権が戻れば、履歴も記憶も本人が見られる状態に復帰する。
  だから管理画面には中身を出していない。出せる画面を作ると、そこが最大の漏洩経路になる
- 発行は所有権を移す操作なので、必ずログに残す

あわせて、プライバシーポリシーの「運営からも見えない作りにしています」という記述を訂正した。
データを預かっている以上、技術的にはアクセスできる。**仕組みで不可能にしているのではなく、
そうしない運用にしている**という区別を、書き分けるようにした。規約の「復旧のご要望にはお応えできません」も
実態と食い違うので書き換えた。

### 9-2. 積み残しの解消（済）

- signalExtractor: 話題の新規性を、実際に初めて出た語かどうかで測るようにした
- 記憶削除: 消し切るまで繰り返すようにした（200件超で残っていた）
- 出会いの図鑑: 12件ずつのページングに
- `/api/health` に管理画面が有効かどうかを出した（合言葉の設定でつまずいたときの切り分け用）

---

## フェーズ10: ドメイン分離と公開まわりの地固め（2026-09-13 実装）

### 10-1. 2つのドメインに分けた（済）

紹介ページ（`/lp`・`/biz`）を apex（waketama.com）の役割にし、サービス本体は app.waketama.com に寄せた。
同じWorkerが両方を受け、`src/hosts.ts` の `hostRedirect()` が振り分ける。

**本体を1つのホストに寄せる**のが要点。持ち主トークンは localStorage にあり、
localStorage はオリジンごとに別物なので、2つのホストでアプリが開けると
片方で育てた分身がもう片方からは「持ち主ではない」ものに見える。利用者から見れば分身の喪失に等しい。

あわせて、canonical リンク・`robots.txt`・`sitemap.xml` をホストごとに出し分けるようにした。
本体側（app）はクロール全面拒否——個人の分身のページがインデックスされて得をする人はいない。

`wrangler dev` はリクエストのURLもHostヘッダも独自ドメインに書き換えるため、ローカルでは
振り分けを無効にしている（`isEdgeRuntime()`）。これを入れる前は、ローカルで `/lp` を開くと
本番へ飛ばされてLPの確認ができなかった。

### 10-2. 404の受け皿（済）

`public/404.html` を用意し、Workerから返すようにした。

途中でアセット層の `not_found_handling = "404-page"` を使おうとしたが、**これは罠だった**。
有効にすると、アセットに無いパスをアセット層がその場で404にしてしまい、リクエストがWorkerまで
届かなくなる——`/api/*` が丸ごと死ぬ。E2Eが「削除後に一覧へ戻らない」「再タップで分身が発行されない」
という形で拾ってくれた。設定は既定に戻し、wrangler.tomlとREADMEに理由を書き残した。
単体テストにも「404ページが出ること」と「同じ経路でAPIが生きていること」を**必ず一緒に**確認する形で追加した。

### 10-3. 問い合わせの二重化と回数制限（済）

`/lp`・`/biz` のフォームは Formspree に送られる（本人が設定済み）。それだけだとメールが1本残るだけで、
返信済みかどうかを追う場所が無い。送信時に同じ内容の控えを `navigator.sendBeacon` で `/api/contact`
にも送り、管理画面で対応状況まで管理できるようにした。控えの送信が失敗しても本来の送信は妨げない。

あわせて `/api/contact` に1日あたりの回数制限を入れた（`migrations/0006_ip_quota.sql`）。
**IPアドレスは保存しない**。その日限りのランダムな塩を足してハッシュ化した値だけを数え、
古い記録は塩ごとCronで消す。数えるための仕組みであって、身元を持つ必要はない。

### 10-4. Service Workerの緊急停止（済。積み残し 0-6 の解消）

壊れたSWを配ると、こちらが直しても端末側の古いSWが動き続ける。
`SW_KILL=1` を付けてデプロイすると `/sw.js` が解除用スクリプトに差し替わり、
端末側でキャッシュを捨てて自分自身を登録解除するようにした。

```bash
npx wrangler deploy --env="" --var SW_KILL:1   # 緊急停止
```

紹介ドメイン（apex）でも同じものを返している。紹介ページからPWAを入れられると、
起動のたびに本体ドメインへ転送される「入れても意味のないアプリ」がホーム画面に並ぶため。
apex では manifest のリンクもHTMLから外している。

### 10-5. 保護のヘッダ（済）

HTMLに `frame-ancestors 'none'` / `X-Frame-Options: DENY` / `nosniff` /
`Referrer-Policy: strict-origin-when-cross-origin` / `Permissions-Policy` を付けた。
管理画面や引き継ぎ画面を透明なiframeで重ねて踏ませる手口を塞ぐのが主目的。

CSPの本体（スクリプトの制限）はまだ入れていない。全ページがインラインスクリプトで書かれているため、
いま入れると `'unsafe-inline'` を許すことになり防御にならない。nonce を配る作りに変えてからにする。

### 10-6. 管理画面に入れないときの案内（済）

`npm run admin:passcode` / `npm run secrets:list` を用意し、404の画面にも
「引数はシークレットの**名前**であること」「`--env=""` が要る理由」「いま応答している環境と版数」を出すようにした。

### 10-7. 残っていること

- CSP（nonce方式への移行）
- 事業者名・連絡先・管轄裁判所の空欄（公開前に必須）
- apex を custom_domain にするため、既存の A/CNAME レコードがあれば先に削除が必要

---

## フェーズ11: 人格データを「売れる状態」にする（2026-09-14 実装）

すべて **人格データの強化と、販売可能な水準への到達** を目的にしている。

### 11-1. ロゴ（勾玉）を作り直した（済）

サービス名の由来（分け御霊）に合わせて、勾玉そのものを標にした。
作図は3つの円だけで決まる: 大きい円の右半分に頭の円を足し、尾の円を引き、頭に紐穴を開ける。
半径を r1 + r2 = R にしておくと3つの円が同じ縦線で接し、頭から尾へ滑らかに細くなる。

同じ作図から、SVG（`public/icons/mark.svg`／HTMLに直接埋め込み）と
PWAアイコン・OGP画像（`tools/make_brand_assets.py` の `magatama_mask`）を生成している。
LPではメインビジュアルの後ろに大きく透かしで置き、キャラクターが勾玉から出てくるように見せた。
**形を変えるときは、SVG・HTML内のpath・Pythonの3箇所を必ず揃えること。**

### 11-2. UIの穴を塞いだ（済）

実機を模した通しの点検（全ページを実際に開いて、出口と閉じるボタンを数える）を行い、次を直した。

- **`/profile` が持ち主には必ず落ちていた**。`renderNotes()` が自分自身を呼んでおり、
  「Maximum call stack size exceeded」で真っ白になっていた。
  持ち主トークンが無い場合は手前の403で止まるため、**持ち主だけが踏む**壊れ方をしていて、
  普通に開いて確認するだけでは見つからなかった。E2Eも403で止まっていたので気づけなかった
- `/summon` でカメラが使えないとき、案内文が説明文とちょうど重なって両方読めなくなっていた（重ねる配置をやめた）
- 行き止まりの解消: `/summon`・`/call`（開始前）・`/market`・`/recover` に戻り口を置いた
- 全ページのロゴを勾玉に差し替えた

### 11-3. パルスサーベイ（段階別に1問ずつ）（済）

会話の合間に**1問ずつ**聞く仕組みを入れた（`src/persona/survey.ts` / `public/pulse.js`）。

- **属性と価値観を交互に出す**。属性だけでは名簿に近く、価値観だけでは心理テストになる。
  売り物になるのは「30代・首都圏」ではなく「30代・首都圏で、安定より刺激を選ぶ人」のほう
- 価値観の設問は、会話からのAI推定と**同じ8軸**に載る形にした。
  本人が答えた軸は `selfReported` に控え、以降のAI推定はその軸をほとんど動かさない（重み 0.3 → 0.06）。
  聞いておいて次の会話で上書きするのでは、答えた意味がなくなる
- 同意が先。無ければ設問の代わりに同意の依頼を返す
- 「あとで」と「もう聞かないで」を別の操作にした。前者を積み上げて後者と解釈しない

### 11-4. 設問を管理画面から変えられるようにした（済）

`survey_questions`（migration 0007）に**上書きと追加だけ**を持ち、組み込みの設問はコードに残した。

- 表が空でも完全に動く（初期化を忘れて設問が全部消える、という壊れ方をしない）
- 同じIDなら上書き、違えば追加。「既定に戻す」で元の文言へ復帰する
- 出したくない設問は削除ではなく無効化（消すと、次の版で復活したときに気づけない）
- 価値観の設問には選択肢ごとに0〜100の点数が要る。点数の無いものは保存させない
  （答えても何も動かない設問になるため）

### 11-5. 人格データの「厚み」（済）

会話の量（40）・属性（25）・価値観（20）・覚えていること（15）で0〜100。**60以上が出品・提供の目安**。
本人には `/profile`、運営には管理画面の人格データ一覧（`depth_score` 列、migration 0008）に出る。

会話だけでは60に届かないようにしてある。たくさん喋っていても「誰なのか」が分からない分身は、
人格データとしては薄い——この判断を、本人と運営が同じ数字で共有できるようにするのが狙い。

### 11-6. 残っていること

- 設問の追加分は、レジストリ上の厚みでは母数に入れていない（実際より控えめに出る。過大に出すよりは安全側）
- 価値観の設問は1軸1問。同じ軸を複数問で測って安定させるのは、実データを見てから
- 出品の水準（60）は暫定値。実際の出品と引き合いを見て調整する

---

## フェーズ12: 「本当に載せ先で動くのか」を測った（2026-09-14）

「集めたデータでフィジカルAIやメタバースのアバターが動くのか」を確かめるため、
検査を先に書いて測った（`tools/persona-runtime-check.mjs`）。**測ったら、動かなかった。**

### 12-1. 分かったこと（測定結果）

正反対の答えをした2体を作り、書き出した人格カードを測った。

| 指標 | 修正前 | 修正後 |
| --- | --- | --- |
| 到達率（カードの情報がプロンプトに現れる割合） | 55% / 48% | **100% / 100%** |
| 価値観8軸がプロンプトに現れた割合 | **0%** | 100% |
| 属性の到達 | 11項目中2〜3 | 11項目中11 |
| 身体のパラメータ | 無し | あり |
| 応答例の健全性 | **5組すべてがAI失敗時の定型文** | 0件 |

**サーベイで集めた価値観は、JSONの中にはあってもプロンプトには1文字も入っていなかった。**
大半の載せ先は `systemPrompt` しか読まないので、これは「集めたが渡していない」状態だった。
設問を作り込んでも、この1箇所が抜けていれば人格データとしての価値はゼロになる。

応答例の件も実害が大きい。AIが落ちたときの「（今はうまく考えがまとまらないみたい…）」が
そのまま口調の手本として持ち出されていた。載せ先のモデルはこれを真似るので、
**受け取った側には「そういう喋り方の分身」に見える**。

### 12-2. 直したこと

- 属性と価値観をプロンプトに載せた。**数値ではなく行動の指示に翻訳して**渡す
  （「刺激: 82」ではモデルは動かない。「定番より先に、やったことがない選択肢を出す」なら動く）
- 本人の申告と会話からの推定を、プロンプト上でも区別した（買い手にとっては、ここが品質そのもの）
- AI失敗時の定型文を応答例から除外した
- `runtime.avatar` を追加（`src/persona/avatarProfile.ts`）。
  動きの大きさ・応答までの間・目線を保つ長さ・対人距離・姿勢、そして
  価値観から導いた振る舞いの方針（`code` は固定の識別子で、日本語を解さない制御系でも分岐できる）

### 12-3. どこまで確かめられていて、どこからが未確認か

**確かめた（自動で回る）**
- 3つの載せ先（LLM／アバター／フィジカルAI）ごとの必須項目が揃っていること
- 集めた情報が漏れなくプロンプトに届いていること
- 正反対の2体が別人として書き出されること（プロンプトの違い61%、価値観の距離52.9）
- **LLMを1回も呼ばずに**、`policy[].code` だけで4場面中4場面の振る舞いが分かれること

**確かめていない**
- **実際のLLMに載せたときの応答**。クラウド開発環境からはモデルの重みを取得できない
  （配信元が許可リストの外）ため、ここは手元で実行する形にした:
  `PERSONA_LLM_URL=http://localhost:11434/v1/chat/completions node tools/persona-runtime-check.mjs`
- 実機のロボット・メタバース上での見え方。数値の対応づけ（`avatarProfile.ts`）は初期値であり、
  実機を見ながら調整する前提
- 小さいモデル（3B以下）で、この長さのプロンプトが破綻しないか。
  プロンプトは約2,000文字あり、載せ先によっては削る必要が出るかもしれない

---

## フェーズ13: 育ちの実感と、入口の整理（2026-09-19 実装）

### 13-1. 成長段階を4→8段に ✅

50回で最上位に着いてしまい、そこから先は何回話しても表示が変わらなかった。
1日に数回話す人なら2週間で打ち止めになる計算で、育てること自体が体験のサービスとしては致命的。

- 段階の間隔を後ろほど広げた（5 → 15 → 35 → 70 → 130 → 220 → 360）
- 段階に意味を持たせた。文脈に載せる会話数・思い出す件数・返事の長さが段階ごとに変わる
- 定義元を `src/ai/growth.ts` 1か所に集約（以前は段階判定と文脈量の閾値が二重に書かれていた）
- チャット画面に進み具合の帯と「あと○回で『××』」を出した

### 13-2. 前回までの会話が画面に戻る ✅

「残さないモードでもないのに履歴が出ない」状態だった。保持数も24発言しかなかった。

- `CharacterState.getDialogue()`（持ち主のみ）と `GET /api/character/history` を追加
- 保持を60発言に拡張。画面には区切り線（「ここまでが前回まで」）を出す

### 13-3. 覚え書きに「本人が書く土台」を足した ✅

会話からの自動学習だけだと、何十ターン話すまで覚え書きが空のままだった。

- `profileNotesSeed`（本人が書く／AIは書き換えない）と `profileNotes`（会話から）を別枠に
- 使うときは必ず `mergeNotes()` で1つにまとめる。片方だけ渡す場所を作ると
  「プロフィールに書いたのに会話では知らない」が起きる
- `/profile` を2段の画面に。書くことに詰まらないよう、ひな形を入れるボタンを置いた
- `POST /api/notes` に `part: "seed" | "learned"`（既定は learned＝既存クライアント互換）

### 13-4. 入口を「依代（よりしろ）」に統一し、QRを加えた ✅

画面の文言が「NFCタグ」固定で、QRで来た人に嘘の案内が出る状態だった。

- 画面の言い方を依代に統一（規約・プライバシーポリシーは定義として1度だけNFCに触れる）
- `/q/:code` を追加。**`/t/:code` と完全に同じ処理**にした
  - 1つの依代から1体だけ／姿はランダム／確率は入口によらず等しい
  - `CharacterState.init()` に見た目を渡す引数は**意図的に持たせていない**
- 「その場所でしか手に入らない」は、姿を固定するのではなく配る場所で作る（`spots`）
- 依代を持たない人の入口 `/add` と `POST /api/character/new`（1日3体まで）
- 管理画面に「依代（タグ・QR）」タブ。発行・CSV書き出し・QR画像の生成まで
- 台帳: `tag_registry` / `spots` / `character_origin`（migration 0009）

### 13-5. 実機で人格データが動くことの確かめ方 ✅

- `tools/edge/compact.mjs`: 人格カード → **数値と識別子だけの数百バイト**（実測 240〜250B）
  - 会話・覚え書き・属性が混ざっていないことを機械で確認してから書き出す
- `tools/edge/rule_runtime.py`: LLMを使わない参照実装。性格の違う2体で
  返すまでの間が 410ms / 930ms、身振りが 2回 / 1回、近づくか否かが分かれることを確認した
- ESP32（Arduino）と Pico（MicroPython）の実装も同梱
- 手順書: `docs/EDGE_DEVICE_TEST.md`（機種ごとの線引き・合格基準・記録票つき）

### 13-6. 「実際に何を売るのか」を決めて実装した ✅

売るのは分身そのものではなく、**その人格を相手の環境で動かせる形にした写し**と、
それを取り出す権利（引換券）。所有権は持ち主のまま動かない。

- 商品は4つ（`src/delivery.ts` の `SKUS` が唯一の定義元。管理画面はそれを表示するだけ）
  - 人格カード / 振る舞いプロファイル（**LLM不要**）/ MCP接続 / 持ち出し一式
- 引換券 `delivery_grants`（migration 0010）: 1体に紐づき、範囲と期限を持つ。
  平文は発行時の1回だけ（保存はハッシュ）。持ち主トークンは買い手に一切渡さない
- 買い手の口 `GET /api/delivery?scope=...&token=...`
- **MCP** `POST /mcp`（`src/mcp.ts`）: JSON-RPC 2.0 over HTTP。読み取りだけで、
  書き込みの道具は作らない。`persona_reply` は保存しない経路なので、
  **買い手が何度話しかけても持ち主の分身は育たない**（テストで担保）
- 管理画面に「納品」タブ。商品一覧・引換券の発行/失効・先方へ送る手順の生成まで
- 書類: `docs/SALES_DELIVERY.md`

### 13-7. 運用マニュアル ✅

`docs/OPERATIONS.md`。毎日/毎週/毎月に見るもの、復旧の手順、依代の配り方、
障害の切り分け、やってはいけないこと（理由つき）。

### 13-8. NFCキーホルダー（50mm角・削り出し）✅

`docs/KEYHOLDER_DESIGN.md` と `docs/keyholder/`（図面SVG 2枚＋DXF）。
図面は `tools/keyholder/make_keyholder.py` が**ロゴと同じ作図から生成**する
（図面だけ手で描くと、ロゴを直したときに現物だけ古い形で残るため）。

設計上の最大の論点は「**金属はNFCを止める**」こと。削り出しとNFCは素のままでは
両立しないので、樹脂で作るか、金属＋フェライトシートにするかを先に決める形にした。

### 積み残し

- キーホルダーの試作と、実機での読み取り確認（フェーズ15でエッジ訴求の追記は済）

---

## フェーズ14: 売り方の言葉を直し、出品を畳み、画面の穴を塞いだ（2026-09-19 実装）

### 14-1. 「自分専用のSLM」と、判断特化AIへの受け渡し ✅

**用語の判断から。**
- **SML は誤り。正しくは SLM**（Small Language Model）。SML は Standard ML という
  プログラミング言語の略称として通っているので、資料に出すと別物にぶつかる
- **Jev は実在する他社製品**（TypeSafe AI、2026年9月発表）。文章を生成せず、
  Choice / Score / Noul の3種類の問いに確率つきの値だけを返す判断特化モデル。
  重みも学習方法も非公開、APIのみ。したがって**「自分専用のJevを作れる」とは言えない**
  （作れないし、他社製品名を自社の生成物として名乗ることになる）。
  しかも Jev は喋らないので、「喋って作る」という体験とも噛み合わない

**そのうえで、相性は良い。** わけたまが会話から取り出しているもの（性格6軸・価値観・方針コード）は、
そのまま「この状況でどちらを選ぶか」という判断の癖で、判断特化モデルが欲しがる入力そのもの。
立ち位置を「Jevを作る」から「**判断特化AIに渡せる形で書き出す**」へ変えた。

- `src/slm.ts`: SLM一式（Modelfile + LoRA学習データ + 手順書）と判断プロファイル
- 納品物に `slm` / `decision` を追加。管理画面の「納品」タブに**下見**も置いた
  （引換券を出さずに、実際に渡るものをその場で見せられる）
- 文言の取り違えはテストで縛った（SMLと書いたら落ちる／Jevを「作れる」と書いたら落ちる）

### 14-2. 出品機能の廃止と、はじめる前の同意 ✅

- **利用者どうしのマーケット（出品・購入申し込み）を削除。**
  人格が他人の手に渡る経路を開けたままにすると、「誰に渡ってよいか」の運用を固める前に
  取り返しのつかない事故が起きうる。企業提供は引換券に一本化した
  （`src/market.ts` → 集約統計だけを `src/insights.ts` へ。`/market` ページとAPI、
  購入希望タブ、`getListingSummary` を削除）
- 同意を作り直した（`CONSENT_VERSION` 2）。`marketplace` を廃止し、**`terms` を追加**
  - 個人情報を預からないこと、**運営が改善や匿名提供に使うことがある**ことへの同意
  - **同意するまで会話を始めさせない**（`public/gate.js`。/summon と /chat の両方に差す）
  - 版を上げたので、既存の同意は取り直しになる（文面が変わったのに流用しない）
- 利用規約・プライバシーポリシーを全面的に書き直し

### 14-3. 会話画面と「これ見て」の修正 ✅

- **キャラクターが常に見えるようにした。** 「たたむ」で高さ0まで消せたため、
  会話が伸びると話し相手が画面から居なくなっていた。たたむ＝小さくする（消えない）に変更し、
  スクロールしても上に残るようにした
- **3Dが描けない端末のための2D差し替え。** WebGLが無い環境では model-viewer は例外も投げずに
  何も描かず、キャラクターの居ない会話画面になっていた。6秒待って読み込めていなければ2Dの絵に切り替える
- **過去の会話は畳んで表示。** 開いた瞬間に古いやり取りで埋まらないよう、直近4件だけを出し、
  残りは「これまでの会話を見る（N件）」の下に入れた
- **「これ見て」が効かなかったのを修正。** 予約ボタン（次に話しかけたときに画像を添える）だったため、
  カメラに何か映して押しても、声を出すまで何も起きなかった。**押した瞬間に送る**形に変更し、
  カメラが使えないときは理由を出すようにした

### 14-4. 分身ごとの声 ✅

種族（5）× 色（6）× 個体 × 性格の4階建てに作り直した。
以前は characterId のハッシュだけで決めていたうえ、会話画面はその値を使わず
その場で性格から計算していたため、**種族も色も違う子が全員ほぼ同じ声で喋っていた**。

- 30通りの組み合わせがすべて違う値になることをテストで担保
- 全体に高め・軽めへ寄せた（下限 1.18）。手のひらサイズの見た目に、低い声は合わない
- 会話画面は、サーバーが決めた値を当てるだけにした（決め方を2箇所に持たない）

### 14-5. テキスト → ロボットの行動 ✅

`docs/TEXT_TO_BEHAVIOR.md`。6段の変換過程、実測値、**限界**（言葉しか観測していない、
対応づけは初期値、会話が浅いと差が出ない）まで書いた。
/lp・/biz・運用マニュアル・納品書類・実機検証手順からも参照している。

### 14-6. サービス紹介動画 ✅

`tools/lp-video/make_video.mjs`。**実際に動かしたサービスを撮って作る。**
画面・キャラクター・成長段階・厚みはすべて本物で、会話の文面だけが紹介用。
作り方をコードにしてあるのは、画面を直したのに動画だけ古い版のまま残るのを防ぐため。

1280×720 / 約26秒 / 1.5MB、`preload="none"` で /lp に設置。

**この動画を作る過程で、`/api/character/history` を新旧2つの用途で取り合っていて、
成長ページが静かに空になっていたことが見つかった**（会話履歴を `/api/character/dialogue` へ改名）。
実画面を撮る作業そのものが、通しの確認として効いた。

---

## フェーズ15: エッジ／スタンドアロン訴求と、実画面の掲載（2026-09-19 実装）

フェーズ13の積み残し「`/lp`・`/biz` へのエッジ／スタンドアロン訴求（実データ・実画面で）」を解消した。

### 15-1. 紹介ページに載せる数字を、測り直した ✅

文書と紹介ページに載っていた実測値（240〜250バイト、410ms / 930ms、±10.8度 / ±2.3度）が
**古くなっていた**。検査用の2体は `npm run persona:fixtures` が毎回その場で作り直しており、
性格の抽出をAIが行うので、同じ回答を入れても値が動く。

測り直した値（2026-09-19）に差し替えたうえで、**「実行のたびに数ポイント動く。差の向きは変わらない」
と併記した**。この断りが無いと、相手が再現したときに数字が合わず、仕組みごと疑われる。

| | さきがけ | ひだまり |
|---|---|---|
| 書き出しの大きさ | 256バイト | 259バイト |
| 返すまでの間 | 458ms | 890ms |
| 待機の揺らぎ | ±10.2度 | ±3.6度 |
| 心地よい距離 | 1.3m | 1.4m |

差し替え先: `docs/TEXT_TO_BEHAVIOR.md`、`docs/SALES_DELIVERY.md`、`docs/OPERATIONS.md`、
`public/lp.html`、`public/biz.html`。再現手順（3コマンド）も文書に書いた。

### 15-2. `/biz` にエッジ向けの節を足した ✅

`#edge`。「動く」の3段階（振る舞い／言葉／常時オフライン）、機種ごとの線引き
（Pi 5 〜 ESP32 の6機種・段階Aは全機種で成立）、**実際の書き出し256バイトの中身**、
参照実装 `rule_runtime.py --diff` の出力、そして限界。

言いたいことは1つ。**喋ることと、その人らしく振る舞うことは別の問題**で、
後者は数値の分岐で足りる。Pico や ESP32 に小さなLLMを載せるのは勧めない、とも明記した。

### 15-3. 実画面の写真を載せた（モックではない）✅

`tools/lp-video/make_shots.mjs`。撮影用の分身と撮り方は紹介動画と共有しており
（`tools/lp-video/demo.mjs` に切り出した）、**動画の中の子とLPに載っている子が別人にならない**。
`public/media/shots/*.webp`（4枚・合計約120KB）。画面を直したら1コマンドで撮り直す。

### 15-4. 撮っている最中に見つかった不具合 ✅

- **成長グラフが、読み込み直後は必ず空だった。** `hidden` のまま `drawChart()` を呼んでおり、
  親要素の `clientWidth` が0。幅0の領域へ描いていた。画面を回すと直るので気づきにくい。
  表示してから描くように直し、E2Eでキャンバスの塗られた画素数を数えて毎回押さえるようにした
- 記録が1回ぶんしか無い分身では、線が引けず白紙に見えていた。グラフごと出さずに理由を書く
- **`/biz` のロゴだけ古い簡略版の勾玉だった**（他14ページは新しい形）。個人向けと法人向けで
  ロゴの形が違っていた。揃えて、E2Eの確認対象に `/biz` を足した
- 撮影用の会話に英単語が1つ紛れていた（「today の分だけでも」）

---

## フェーズ5: 公開に向けた運用・法務

一般公開するなら機能より先に必要になる項目です。

- ~~**プライバシーポリシー**~~: 作成済み（`public/privacy.html` → `/privacy`）。会話・音声・カメラ映像・
  「これ見て」で送る静止画の扱いまで書いてあります。**ただし下書きであり、事業者名と連絡先が空欄のままです。
  公開前に埋めたうえで、弁護士等の確認を受けてください**
- ~~**利用規約**~~: 作成済み（`public/terms.html` → `/terms`）。禁止事項・免責・分身データの扱い・
  マーケット・サービス終了時の扱いまで含めてあります。**ただし下書きで、事業者名・連絡先・管轄裁判所が空欄です**
- **年齢への配慮**: NFCキーホルダーという形状は子どもも触ります。表現と収集情報の見直し
- **不適切な入力への対応**: 現在はモデル任せ。方針を決める
- ~~**問い合わせ窓口**~~: 実施済み。法人向け（`/biz`）と復旧（`/recover`）の窓口を用意し、
  届いたものは管理画面で対応状況まで管理できる

---

## 残っている作業（優先度順）

1. **実機での音声確認**: 音声入力はコードとしては入っているが、iOS Safari / Android Chromeでの
   挙動は実機でしか分からない。マイク権限、無音時の扱い、ハンズフリーの回り方を確認する
2. **利用規約とプライバシーポリシーの空欄埋め**: 文書は用意済み。事業者名・連絡先・管轄裁判所を
   埋めたうえで、弁護士等の確認を受けてください（公開前に必須）
3. **読み上げの本採用**: `/api/voice/speak`（MeloTTS）は用意したが、日本語の品質と遅延を
   実際に聞いてから、ブラウザ標準と置き換えるか選択制のままにするかを決める
4. ~~**Service Workerの緊急停止手段**（0-6）~~ 実装済み（10-4）
5. **iOS向けのホーム画面追加の案内**
6. **性格更新ロジックのチューニング**（実ログを見ながら。観測性が入ったので着手可能になった）
7. **CSPの本採用**: いまは frame-ancestors だけ。スクリプトの制限は nonce 方式に変えてから（10-5）

## 進める順番の提案（当初計画）

```
フェーズ0（0-1〜0-3）  ← ドメイン反映後すぐ。ここが無いと以降のデバッグが苦しい
   ↓
フェーズ2（2-1・2-2）  ← 「残さない」土台。テストを先に書く
   ↓
フェーズ2（2-3）＋フェーズ1（1-1）  ← ストリーミングと音声入力
   ↓
フェーズ2（2-4）      ← 通話UIとして統合。ここで「分身と電話する」が完成
   ↓
フェーズ0（0-7）＋フェーズ5  ← 公開前に必須
   ↓
フェーズ3・4          ← 継続的な改善
```

音声とその場限りモードは最終的にひとつの体験（分身との通話）に合流するので、
別々に作り込むより、上記の順で土台から積むほうが手戻りが少なくなります。
